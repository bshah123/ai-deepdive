---
id: "7.4"
part: 1
chapter: 7
title: "Recursion Limits & Stack Overflow Prevention"
slug: "recursion-stack"
difficulty: "intermediate"
estimated_minutes: 20
prerequisites: []
tags: ["recursion", "sys-setrecursionlimit", "stack"]
status: "published"
---

# 7.4 — Recursion Limits & Stack Overflow Prevention

## Opening Motivation & Context

Recursion is a powerful programming technique where a function calls itself to solve a problem by breaking it down into smaller subproblems. However, recursion has its limits, and exceeding these limits can lead to a stack overflow, which is a critical issue in computing and AI systems. Understanding these limits and how to prevent stack overflows is essential for writing robust and efficient recursive functions.

## Core Concepts Breakdown

### Recursion Limits

Recursion limits refer to the maximum depth of recursive calls that a programming language allows before it raises a stack overflow error. Each recursive call adds a new frame to the call stack, and when the maximum depth is reached, the stack overflows.

> [!KEY-INSIGHT]
> The call stack has a finite size, and each recursive call consumes a portion of this stack. Understanding the recursion limits helps in designing algorithms that do not exceed these limits.

> [!BEST-PRACTICE]
> Always include a base case in your recursive function to ensure that the recursion terminates. Additionally, consider using iterative approaches for problems that require deep recursion to avoid stack overflow.

> [!WARNING]
> Exceeding the recursion limits can lead to a stack overflow, which can crash your program or, in severe cases, the entire system. Always ensure that your recursive function has a clear termination condition.

## Code Demonstration & Runtime Output

```python
import sys

def recursive_function(n: int) -> int:
    """
    A recursive function that calculates the factorial of a number.

    Args:
        n (int): The number to calculate the factorial for.

    Returns:
        int: The factorial of the number.
    """
    if n == 0:
        return 1
    else:
        return n * recursive_function(n - 1)

def main() -> None:
    """
    The main function that demonstrates the recursive function and handles recursion limits.
    """
    try:
        # Get the recursion limit
        recursion_limit = sys.getrecursionlimit()
        print(f"Current recursion limit: {recursion_limit}")

        # Set a new recursion limit
        new_limit = 1000
        sys.setrecursionlimit(new_limit)
        print(f"New recursion limit set to: {new_limit}")

        # Calculate factorial of a number
        num = 5
        result = recursive_function(num)
        print(f"The factorial of {num} is {result}")

        # Attempt to calculate factorial of a large number to demonstrate stack overflow
        large_num = 1001
        result = recursive_function(large_num)
        print(f"The factorial of {large_num} is {result}")
    except RecursionError as e:
        print(f"RecursionError: {e}")

if __name__ == "__main__":
    main()
```

Output:
```
Current recursion limit: 1000
New recursion limit set to: 1000
The factorial of 5 is 120
RecursionError: maximum recursion depth exceeded in comparison
```

### How this works (Line-by-Line Breakdown)

1. **Importing the sys module**: The `sys` module is imported to access system-specific parameters and functions, including the recursion limit.
2. **Defining the recursive function**: The `recursive_function` calculates the factorial of a number using recursion. It has a base case (`n == 0`) to terminate the recursion and a recursive case (`n * recursive_function(n - 1)`) to break down the problem into smaller subproblems.
3. **Defining the main function**: The `main` function demonstrates the recursive function and handles recursion limits.
4. **Getting the current recursion limit**: The `sys.getrecursionlimit()` function is used to get the current recursion limit.
5. **Setting a new recursion limit**: The `sys.setrecursionlimit()` function is used to set a new recursion limit.
6. **Calculating factorial of a number**: The `recursive_function` is called with a small number to demonstrate that it works correctly.
7. **Attempting to calculate factorial of a large number**: The `recursive_function` is called with a large number to demonstrate a stack overflow.
8. **Handling recursion errors**: A `try-except` block is used to catch and handle the `RecursionError` that occurs when the recursion limit is exceeded.
9. **Running the main function**: The `if __name__ == "__main__":` block ensures that the `main` function is called when the script is run directly.

> [!ADVANCED]
> The recursion limit is implemented in CPython as a counter in the Python interpreter's C code. Each time a function is called, the counter is incremented, and when it reaches the recursion limit, a `RecursionError` is raised. The recursion limit can be adjusted using the `sys.setrecursionlimit()` function, but it is generally not recommended to increase it beyond the default value.

## Q&A

### Q: When should I prefer iteration over recursion?

<details><summary>Show Solution</summary>

Iteration is generally preferred over recursion when the problem can be solved with a simple loop, as it is more efficient and avoids the overhead of recursive function calls. Recursion is better suited for problems that can be broken down into smaller subproblems that are similar to the original problem, such as tree and graph traversals.

</details>

## Summary & Key Takeaways

- Recursion limits refer to the maximum depth of recursive calls that a programming language allows.
- Each recursive call adds a new frame to the call stack, and when the maximum depth is reached, the stack overflows.
- Always include a base case in your recursive function to ensure that the recursion terminates.
- Consider using iterative approaches for problems that require deep recursion to avoid stack overflow.
- The recursion limit can be adjusted using the `sys.setrecursionlimit()` function, but it is generally not recommended to increase it beyond the default value.

## Quiz Time

### Question #1: What is the purpose of the base case in a recursive function?

<details><summary>Show Solution</summary>

The base case in a recursive function is used to terminate the recursion. It provides a solution to the simplest instance of the problem, which allows the recursive calls to build upon this solution to solve larger instances of the problem.

</details>

### Question #2: What does the following code output?

```python
def recursive_function(n: int) -> int:
    if n == 0:
        return 1
    else:
        return n * recursive_function(n - 1)

def main() -> None:
    result = recursive_function(3)
    print(result)

if __name__ == "__main__":
    main()
```

<details><summary>Show Solution</summary>

The code outputs `6`, which is the factorial of 3.

</details>

### Question #3: Find and fix the bug in the following code:

```python
def recursive_function(n: int) -> int:
    if n == 0:
        return 1
    else:
        return n * recursive_function(n)

def main() -> None:
    result = recursive_function(5)
    print(result)

if __name__ == "__main__":
    main()
```

<details><summary>Show Solution</summary>

The bug in the code is that the recursive call `recursive_function(n)` does not reduce the problem size, leading to infinite recursion. The fixed code is as follows:

```python
def recursive_function(n: int) -> int:
    if n == 0:
        return 1
    else:
        return n * recursive_function(n - 1)

def main() -> None:
    result = recursive_function(5)
    print(result)

if __name__ == "__main__":
    main()
```

</details>
