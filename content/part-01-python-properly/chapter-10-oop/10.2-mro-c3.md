---
id: "10.2"
part: 1
chapter: 10
title: "Method Resolution Order (MRO) & C3 Linearization"
slug: "mro-c3"
difficulty: "advanced"
estimated_minutes: 30
prerequisites: []
tags: ["mro", "c3", "inheritance"]
status: "published"
---

# 10.2 — Method Resolution Order (MRO) & C3 Linearization

## Opening Motivation & Context

In Python's object-oriented programming paradigm, the Method Resolution Order (MRO) is a critical concept that determines the order in which base classes are searched when executing a method. This is particularly important in multiple inheritance scenarios, where a class can inherit from more than one parent class. The C3 Linearization algorithm is the method Python uses to calculate the MRO, ensuring a consistent and predictable order.

Understanding MRO is essential for writing maintainable and bug-free code, especially when dealing with complex class hierarchies. It helps developers predict which method will be called when dealing with method overriding and inheritance.

## Core Concepts Breakdown

### Method Resolution Order (MRO)

The MRO is the order in which Python looks for a method in a hierarchy of classes. When you call a method on an instance of a class, Python searches for the method in the instance's class, then in the base classes, following the MRO.

> [!KEY-INSIGHT]
> The MRO is determined using the C3 Linearization algorithm, which ensures a consistent and predictable order, even in complex inheritance hierarchies.

### C3 Linearization

C3 Linearization is the algorithm used by Python to calculate the MRO. It ensures that the order is:
1. Monotonic: A class always appears before its parents.
2. Extending: If a class `C` inherits from classes `A` and `B`, and `A` appears before `B` in the MRO of `C`, then `A` must appear before `B` in the MRO of any subclass of `C`.
3. Local precedence: The order of classes in the inheritance list is preserved.

> [!BEST-PRACTICE]
> When designing class hierarchies, consider the MRO to ensure that method calls behave as expected. Use the `mro()` method to inspect the MRO of a class.

> [!WARNING]
> Diamond inheritance, where a class inherits from two classes that have a common ancestor, can lead to unexpected behavior if the MRO is not understood. Always inspect the MRO when dealing with complex inheritance hierarchies.

## Code Demonstration & Runtime Output

```python
class A:
    def show(self):
        print("A.show()")

class B(A):
    def show(self):
        print("B.show()")

class C(A):
    def show(self):
        print("C.show()")

class D(B, C):
    def show(self):
        print("D.show()")

if __name__ == "__main__":
    d = D()
    d.show()
    print(D.mro())
```

Output:
```
D.show()
[<class '__main__.D'>, <class '__main__.B'>, <class '__main__.C'>, <class '__main__.A'>, <class 'object'>]
```

### How this works (Line-by-Line Breakdown)

1. **Class Definitions**:
   - `class A`: Defines a base class `A` with a method `show()`.
   - `class B(A)`: Defines a class `B` that inherits from `A` and overrides the `show()` method.
   - `class C(A)`: Defines a class `C` that inherits from `A` and overrides the `show()` method.
   - `class D(B, C)`: Defines a class `D` that inherits from both `B` and `C` and overrides the `show()` method.

2. **Main Block**:
   - `if __name__ == "__main__":`: Ensures the code runs only when the script is executed directly.
   - `d = D()`: Creates an instance of class `D`.
   - `d.show()`: Calls the `show()` method on the instance `d`, which prints "D.show()".
   - `print(D.mro())`: Prints the MRO of class `D`, showing the order in which base classes are searched.

> [!ADVANCED]
> The MRO is calculated using the C3 Linearization algorithm, which ensures a consistent and predictable order. The `mro()` method returns a tuple of classes in the order they are searched for method resolution.

## Summary & Key Takeaways

- The MRO determines the order in which base classes are searched when executing a method.
- C3 Linearization is the algorithm used by Python to calculate the MRO.
- The MRO ensures that the order is monotonic, extending, and preserves local precedence.
- Use the `mro()` method to inspect the MRO of a class.
- Diamond inheritance can lead to unexpected behavior if the MRO is not understood.

## Quiz Time

### Question #1: Conceptual Question

What is the purpose of the Method Resolution Order (MRO) in Python?

<details><summary>Show Solution</summary>
The MRO determines the order in which base classes are searched when executing a method, ensuring a consistent and predictable order, especially in multiple inheritance scenarios.
</details>

### Question #2: What does this code output?

```python
class X:
    def show(self):
        print("X.show()")

class Y(X):
    def show(self):
        print("Y.show()")

class Z(Y):
    def show(self):
        print("Z.show()")

if __name__ == "__main__":
    z = Z()
    z.show()
    print(Z.mro())
```

<details><summary>Show Solution</summary>
The code outputs:
```
Z.show()
[<class '__main__.Z'>, <class '__main__.Y'>, <class '__main__.X'>, <class 'object'>]
```
</details>

### Question #3: Find and fix the bug

```python
class P:
    def show(self):
        print("P.show()")

class Q(P):
    def show(self):
        print("Q.show()")

class R(P):
    def show(self):
        print("R.show()")

class S(Q, R):
    def show(self):
        print("S.show()")

if __name__ == "__main__":
    s = S()
    s.show()
    print(S.mro())
```

<details><summary>Show Solution</summary>
The code is correct and outputs:
```
S.show()
[<class '__main__.S'>, <class '__main__.Q'>, <class '__main__.R'>, <class '__main__.P'>, <class 'object'>]
```
</details>
