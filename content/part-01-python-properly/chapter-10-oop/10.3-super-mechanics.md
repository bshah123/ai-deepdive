---
id: "10.3"
part: 1
chapter: 10
title: "super() Mechanics & Cooperative Multiple Inheritance"
slug: "super-mechanics"
difficulty: "advanced"
estimated_minutes: 25
prerequisites: []
tags: ["super", "cooperative-inheritance"]
status: "published"
---

# 10.3 — super() Mechanics & Cooperative Multiple Inheritance

## Opening Motivation & Context

In Python's object-oriented programming paradigm, the `super()` function and cooperative multiple inheritance are powerful tools that enable developers to create flexible, reusable, and maintainable code. Understanding these concepts is crucial for building complex systems, especially in the context of artificial intelligence and software engineering. The `super()` function allows you to access methods and attributes from a parent class, while cooperative multiple inheritance ensures that method resolution order (MRO) is handled correctly when multiple parent classes are involved.

## Core Concepts Breakdown

### The `super()` Function

The `super()` function is used to delegate method calls to the parent class. It is particularly useful in cooperative multiple inheritance scenarios where you need to ensure that all parent classes are properly initialized and their methods are called in the correct order.

> [!KEY-INSIGHT]
> The `super()` function dynamically determines the parent class to call, based on the current class and the instance's method resolution order (MRO). This dynamic behavior is what makes `super()` so powerful in cooperative multiple inheritance.

> [!BEST-PRACTICE]
> Always use `super()` in the `__init__` method of a child class to ensure that all parent classes are properly initialized. This practice helps avoid common bugs and ensures that the object is in a valid state.

> [!WARNING]
> Misusing `super()` can lead to unexpected behavior, such as skipping parent class initialization or calling methods in the wrong order. Always ensure that `super()` is used correctly in cooperative multiple inheritance scenarios.

### Cooperative Multiple Inheritance

Cooperative multiple inheritance is a technique where multiple parent classes work together to provide functionality to a child class. This is achieved by carefully designing the parent classes and using `super()` to ensure that methods are called in the correct order.

> [!KEY-INSIGHT]
> Cooperative multiple inheritance relies on the method resolution order (MRO) to determine the order in which methods are called. The MRO is a tuple that specifies the order in which parent classes are searched for attributes and methods.

> [!BEST-PRACTICE]
> Design parent classes to be cooperative and avoid deep inheritance hierarchies. This practice helps keep the codebase maintainable and reduces the risk of bugs.

> [!WARNING]
> Deep inheritance hierarchies can lead to complex and hard-to-maintain code. Always strive to keep the inheritance hierarchy as flat as possible.

## Code Demonstration & Runtime Output

```python
class ParentA:
    def __init__(self) -> None:
        print("ParentA.__init__")
        self.a = "ParentA"

    def method(self) -> None:
        print("ParentA.method")

class ParentB:
    def __init__(self) -> None:
        print("ParentB.__init__")
        self.b = "ParentB"

    def method(self) -> None:
        print("ParentB.method")

class Child(ParentA, ParentB):
    def __init__(self) -> None:
        print("Child.__init__")
        super().__init__()
        self.c = "Child"

    def method(self) -> None:
        print("Child.method")
        super().method()

if __name__ == "__main__":
    child = Child()
    print(child.a)
    print(child.b)
    print(child.c)
    child.method()
```

Output:
```
Child.__init__
ParentA.__init__
ParentB.__init__
ParentA
ParentB
Child
Child.method
ParentA.method
```

## How this works (Line-by-Line Breakdown)

1. **ParentA Class Definition**: The `ParentA` class is defined with an `__init__` method that initializes an attribute `a` and a `method` that prints a message.
2. **ParentB Class Definition**: The `ParentB` class is defined with an `__init__` method that initializes an attribute `b` and a `method` that prints a message.
3. **Child Class Definition**: The `Child` class inherits from both `ParentA` and `ParentB`. Its `__init__` method calls `super().__init__()` to ensure that both parent classes are properly initialized. It also initializes an attribute `c`.
4. **Child Method Definition**: The `Child` class defines a `method` that prints a message and calls `super().method()` to ensure that the parent class methods are called in the correct order.
5. **Main Block**: The `if __name__ == "__main__":` block creates an instance of the `Child` class and calls its `method`. The output shows the order in which the methods are called, demonstrating the cooperative multiple inheritance.

## Advanced

In CPython, the `super()` function is implemented using the `super` type, which is a built-in type that provides access to the parent class. The `super` type uses the method resolution order (MRO) to determine the parent class to call. The MRO is a tuple that specifies the order in which parent classes are searched for attributes and methods. The MRO is calculated using the C3 linearization algorithm, which ensures that the MRO is consistent and predictable.

## Q&A

### Q: When should I prefer cooperative multiple inheritance over single inheritance?

A: Cooperative multiple inheritance should be preferred when you need to combine functionality from multiple parent classes. However, it is important to design the parent classes to be cooperative and avoid deep inheritance hierarchies. Single inheritance should be preferred when the functionality can be provided by a single parent class.

## Summary & Key Takeaways

- The `super()` function is used to delegate method calls to the parent class.
- Cooperative multiple inheritance ensures that methods are called in the correct order.
- Always use `super()` in the `__init__` method of a child class to ensure that all parent classes are properly initialized.
- Design parent classes to be cooperative and avoid deep inheritance hierarchies.
- The method resolution order (MRO) determines the order in which methods are called.

## Quiz time

### Question #1: What is the method resolution order (MRO) in the `Child` class?

<details><summary>Show Solution</summary>

The MRO in the `Child` class is `(Child, ParentA, ParentB, object)`. This means that when searching for attributes and methods, the `Child` class is searched first, followed by `ParentA`, `ParentB`, and finally `object`.

</details>

### Question #2: What does the following code output?

```python
class ParentA:
    def method(self) -> None:
        print("ParentA.method")

class ParentB:
    def method(self) -> None:
        print("ParentB.method")

class Child(ParentA, ParentB):
    def method(self) -> None:
        print("Child.method")
        super().method()

child = Child()
child.method()
```

<details><summary>Show Solution</summary>

The code outputs:
```
Child.method
ParentA.method
```

This is because the `Child` class's `method` calls `super().method()`, which delegates the call to the next class in the MRO, which is `ParentA`.

</details>

### Question #3: Find and fix the bug in the following code:

```python
class ParentA:
    def __init__(self) -> None:
        print("ParentA.__init__")

class ParentB:
    def __init__(self) -> None:
        print("ParentB.__init__")

class Child(ParentA, ParentB):
    def __init__(self) -> None:
        print("Child.__init__")
        self.c = "Child"

if __name__ == "__main__":
    child = Child()
```

<details><summary>Show Solution</summary>

The bug in the code is that the `Child` class's `__init__` method does not call `super().__init__()`, which means that the parent classes are not properly initialized. The fixed code is:

```python
class ParentA:
    def __init__(self) -> None:
        print("ParentA.__init__")

class ParentB:
    def __init__(self) -> None:
        print("ParentB.__init__")

class Child(ParentA, ParentB):
    def __init__(self) -> None:
        print("Child.__init__")
        super().__init__()
        self.c = "Child"

if __name__ == "__main__":
    child = Child()
```

The fixed code outputs:
```
Child.__init__
ParentA.__init__
ParentB.__init__
```

This is because the `Child` class's `__init__` method now calls `super().__init__()`, which ensures that both parent classes are properly initialized.

</details>
