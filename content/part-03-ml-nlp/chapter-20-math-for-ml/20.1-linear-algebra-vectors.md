---
id: "20.1"
part: 3
chapter: 20
title: "Linear Algebra: Vector Spaces, Dot Products & Projections"
slug: "linear-algebra"
difficulty: "intermediate"
estimated_minutes: 25
prerequisites: []
tags: ["vectors", "dot-product", "projections"]
status: "published"
---

# 20.1 — Linear Algebra: Vector Spaces, Dot Products & Projections

## Opening Motivation & Context

Linear algebra is the mathematical foundation of machine learning and natural language processing. It provides the tools to represent and manipulate data in high-dimensional spaces, which is essential for tasks like text classification, sentiment analysis, and language translation.

In this lesson, we'll explore three fundamental concepts: vector spaces, dot products, and projections. These concepts are crucial for understanding how machine learning algorithms process and learn from data.

## Core Concepts Breakdown

### Vector Spaces

A vector space is a collection of vectors that can be added together and multiplied by scalars (real numbers) while satisfying certain axioms. In machine learning, vectors are used to represent data points, and vector spaces are used to represent the space in which these data points exist.

> [!KEY-INSIGHT]
> A vector space is a mathematical structure that allows us to represent and manipulate data in a way that is compatible with the operations of machine learning algorithms.

> [!BEST-PRACTICE]
> When working with vector spaces in machine learning, it's important to ensure that your data is properly normalized and scaled to avoid numerical instability and improve the performance of your algorithms.

### Dot Products

The dot product (also known as the scalar product) is an operation that takes two vectors and returns a single number. It is calculated by multiplying the corresponding components of the vectors and then summing the results. The dot product is used in machine learning to measure the similarity between two vectors and to compute the gradient of a function.

> [!KEY-INSIGHT]
> The dot product is a fundamental operation in linear algebra that is used extensively in machine learning to measure similarity and compute gradients.

> [!WARNING]
> The dot product is not commutative, meaning that the order of the vectors matters. This can lead to bugs if you accidentally swap the order of the vectors in your code.

### Projections

Projection is an operation that takes a vector and projects it onto another vector or subspace. The projection of a vector onto another vector is the component of the first vector that lies in the direction of the second vector. Projections are used in machine learning to reduce the dimensionality of data and to find the best-fit line or plane for a given set of data points.

> [!KEY-INSIGHT]
> Projection is a powerful tool in linear algebra that allows us to reduce the dimensionality of data and find the best-fit line or plane for a given set of data points.

> [!BEST-PRACTICE]
> When working with projections in machine learning, it's important to ensure that your data is properly centered and scaled to avoid numerical instability and improve the performance of your algorithms.

## Code Demonstration & Runtime Output

Let's demonstrate these concepts with a Python code snippet that calculates the dot product and projection of two vectors.

```python
import numpy as np

def dot_product(v1: np.ndarray, v2: np.ndarray) -> float:
    """
    Calculate the dot product of two vectors.

    Args:
        v1: First vector.
        v2: Second vector.

    Returns:
        Dot product of v1 and v2.
    """
    return np.sum(v1 * v2)

def projection(v1: np.ndarray, v2: np.ndarray) -> np.ndarray:
    """
    Calculate the projection of v1 onto v2.

    Args:
        v1: Vector to be projected.
        v2: Vector onto which v1 is projected.

    Returns:
        Projection of v1 onto v2.
    """
    return (dot_product(v1, v2) / dot_product(v2, v2)) * v2

if __name__ == "__main__":
    # Define two vectors
    v1 = np.array([1, 2, 3])
    v2 = np.array([4, 5, 6])

    # Calculate the dot product
    dp = dot_product(v1, v2)
    print(f"Dot product of v1 and v2: {dp}")

    # Calculate the projection of v1 onto v2
    proj = projection(v1, v2)
    print(f"Projection of v1 onto v2: {proj}")
```

```text
Output:
Dot product of v1 and v2: 32
Projection of v1 onto v2: [1.6 2.  2.4]
```

### How this works (Line-by-Line Breakdown)

1. `import numpy as np`: Import the NumPy library, which provides support for arrays and mathematical operations.
2. `def dot_product(v1: np.ndarray, v2: np.ndarray) -> float:`: Define a function to calculate the dot product of two vectors.
3. `return np.sum(v1 * v2)`: Calculate the dot product by multiplying the corresponding components of the vectors and then summing the results.
4. `def projection(v1: np.ndarray, v2: np.ndarray) -> np.ndarray:`: Define a function to calculate the projection of one vector onto another.
5. `return (dot_product(v1, v2) / dot_product(v2, v2)) * v2`: Calculate the projection by scaling the second vector by the ratio of the dot product of the two vectors to the dot product of the second vector with itself.
6. `if __name__ == "__main__":`: Check if the script is being run as the main program.
7. `v1 = np.array([1, 2, 3])`: Define the first vector.
8. `v2 = np.array([4, 5, 6])`: Define the second vector.
9. `dp = dot_product(v1, v2)`: Calculate the dot product of the two vectors.
10. `print(f"Dot product of v1 and v2: {dp}")`: Print the dot product.
11. `proj = projection(v1, v2)`: Calculate the projection of the first vector onto the second vector.
12. `print(f"Projection of v1 onto v2: {proj}")`: Print the projection.

> [!ADVANCED]
> The dot product and projection operations are implemented using NumPy arrays, which are stored in contiguous memory blocks and optimized for mathematical operations. The `np.sum` function is used to calculate the sum of the elements in an array, and the `*` operator is used to multiply the corresponding elements of two arrays.

> [!QA]
> Q: When should I prefer using NumPy arrays over Python lists for vector operations?
> A: NumPy arrays are more efficient and optimized for mathematical operations, making them the preferred choice for vector operations in machine learning. Additionally, NumPy provides a wide range of functions for working with arrays, such as `np.sum`, `np.dot`, and `np.linalg.norm`, which can simplify your code and improve its performance.

## Summary & Key Takeaways

- Vector spaces are used to represent the space in which data points exist.
- The dot product is a fundamental operation in linear algebra that is used extensively in machine learning to measure similarity and compute gradients.
- Projection is a powerful tool in linear algebra that allows us to reduce the dimensionality of data and find the best-fit line or plane for a given set of data points.
- NumPy arrays are more efficient and optimized for mathematical operations, making them the preferred choice for vector operations in machine learning.

## Quiz time

### Question #1: Conceptual question

What is the difference between a vector space and a subspace?

<details><summary>Show Solution</summary>

A vector space is a collection of vectors that can be added together and multiplied by scalars (real numbers) while satisfying certain axioms. A subspace is a subset of a vector space that is also a vector space in its own right. In other words, a subspace is a vector space that is contained within another vector space.

</details>

### Question #2: What does this code output?

```python
import numpy as np

def dot_product(v1: np.ndarray, v2: np.ndarray) -> float:
    return np.sum(v1 * v2)

if __name__ == "__main__":
    v1 = np.array([1, 2, 3])
    v2 = np.array([4, 5, 6])
    dp = dot_product(v1, v2)
    print(f"Dot product of v1 and v2: {dp}")
```

<details><summary>Show Solution</summary>

The code outputs the dot product of the two vectors, which is 32.

</details>

### Question #3: Find and fix the bug

```python
import numpy as np

def projection(v1: np.ndarray, v2: np.ndarray) -> np.ndarray:
    return (dot_product(v1, v2) / dot_product(v2, v2)) * v2

if __name__ == "__main__":
    v1 = np.array([1, 2, 3])
    v2 = np.array([4, 5, 6])
    proj = projection(v1, v2)
    print(f"Projection of v1 onto v2: {proj}")
```

<details><summary>Show Solution</summary>

The bug in the code is that the `dot_product` function is not defined. To fix the bug, you need to define the `dot_product` function before it is used in the `projection` function.

```python
import numpy as np

def dot_product(v1: np.ndarray, v2: np.ndarray) -> float:
    return np.sum(v1 * v2)

def projection(v1: np.ndarray, v2: np.ndarray) -> np.ndarray:
    return (dot_product(v1, v2) / dot_product(v2, v2)) * v2

if __name__ == "__main__":
    v1 = np.array([1, 2, 3])
    v2 = np.array([4, 5, 6])
    proj = projection(v1, v2)
    print(f"Projection of v1 onto v2: {proj}")
```

</details>
