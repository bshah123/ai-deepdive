---
id: "20.1"
part: 3
chapter: 20
title: "Linear Algebra: Vector Spaces, Dot Products & Projections"
slug: "linear-algebra"
difficulty: "intermediate"
estimated_minutes: 25
prerequisites: []
tags: ["vectors", "dot-product", "projections"]
status: "published"
---

# Architectural Intuition & Execution Pipeline
Understanding vectors, dot products, and projections is akin to visualizing data flowing through a **pipeline of linear transformations**. Each stage reshapes the data without altering its intrinsic dimensionality, much like a series of micro‑services that pass immutable messages. The diagram below abstracts this flow:

*The quoted node labels keep the diagram parsable by Mermaid.*

---

# Low-Level Systems Anatomy & Execution Mechanics
### CPython & NumPy Internals
- **`PyArrayObject`** (NumPy) stores a pointer to a contiguous memory buffer (`data`), a `ndim` count, and a `strides` tuple that maps logical indices to byte offsets.  
- **Reference counting** (`ob_refcnt`) ensures tensors are freed only when the last Python object releases it.

### PyTorch Tensor Layout
- Tensors are wrappers around **`c10::TensorImpl`** which holds:
  - `storage` (raw memory, often a `c10::StorageImpl` on CPU or CUDA)
  - `sizes` and `strides` (int64 vectors) that define the **row‑major** or **column‑major** view.
- **Strides** enable *zero‑copy* views for projections: a projection can be expressed as a tensor with altered strides rather than copying data.

### CUDA / Triton Kernel Sketch for Dot Product
*The kernel demonstrates how a dot product can be parallelized across GPU warps, a pattern reused in transformer attention heads.*

### KaTeX Formulas
The core mathematics:

$$
\mathbf{a} \cdot \mathbf{b} = \sum_{i=1}^{n} a_i b_i
$$

$$
\operatorname{proj}_{\mathbf{b}}(\mathbf{a}) = \frac{\mathbf{a} \cdot \mathbf{b}}{\mathbf{b} \cdot \mathbf{b}} \, \mathbf{b}
$$

---

# Production Implementation & Interactive Code Lab
---

# Step-by-Step Runtime Memory & State Trace
| Step | Variable | Type | Shape / Size | Memory Location | Value Snapshot |
|------|----------|------|--------------|----------------|----------------|
| 1 | `v1` | `Vector` | (3,) | Python heap | `(1.0, 2.0, 3.0)` |
| 2 | `v1._np` | `np.ndarray` | (3,) | C‑contiguous buffer | `[1., 2., 3.]` |
| 3 | `v2` | `Vector` | (3,) | Python heap | `(4.0, -5.0, 6.0)` |
| 4 | `v2._np` | `np.ndarray` | (3,) | C‑contiguous buffer | `[4., -5., 6.]` |
| 5 | `v1.dot(v2)` | `float` | scalar | Stack | `12.0` |
| 6 | `proj` | `Vector` | (3,) | Python heap | `(0.6153846153846154, -0.7692307692307693, 0.9230769230769231)` |
| 7 | `batch` | `torch.Tensor` | (3,3) | CPU pinned memory | `[[1,2,3],[4,5,6],[7,8,9]]` |
| 8 | `dots` | `torch.Tensor` | (3,) | CPU memory | `[14., 77., 194.]` |
| 9 | `batch_gpu` (optional) | `torch.Tensor` | (3,3) | GPU VRAM | same as `batch` |
| 10 | `dots_gpu` (optional) | `torch.Tensor` | (3,) | GPU VRAM | `[14., 77., 194.]` |

---

# Hard-Won Production Traps & Antipatterns
> [!TRAP] **Zero‑Division in Projections**  
When the base vector’s norm is near‑zero, `base.dot(base)` underflows, causing a `ZeroDivisionError` or, worse, a silent `inf` scale factor. Guard with `math.isclose(denom, 0.0, rel_tol=1e-12)` and fallback to a zero vector.

> [!TRAP] **Strides Mis‑alignment in PyTorch Views**  
Creating a projection by reshaping tensors without adjusting strides can lead to **unexpected broadcasting** and memory corruption. Always use `torch.as_strided` with explicit stride calculations or rely on `torch.nn.functional.normalize` followed by `torch.matmul` to stay safe.

---

# Systems Benchmarking & Performance Profile
### Complexity
- **Dot product**: \(O(n)\) time, \(O(1)\) extra space.  
- **Projection**: Two dot products + a scalar‑vector multiplication → \(O(n)\) time, \(O(1)\) extra space.

### Profiling Recipe (CPU)
Inspect hot spots with `snakeviz profile.txt`.

### Memory Tracking (Python)
### GPU Profiling (torch.profiler)
The trace reveals kernel launch overhead vs compute intensity.

---

# Real-World Frontier AI Connection
> [!AI] **Attention Scores as Batched Dot Products**  
Transformer attention computes \(QK^\top\) where each row of \(Q\) and \(K\) are query/key vectors. This is a massive batched dot‑product operation, heavily optimized with fused CUDA kernels (e.g., FlashAttention). Mastering vector dot products and projections directly translates to understanding and tuning these attention mechanisms in LLMs like GPT‑4 or vLLM serving stacks.

---

# Hands-on Engineering Crucible
### 🟢 Beginner
*Task*: Extend the `Vector` class with a `cross` method for 3‑D vectors. Verify orthogonality by checking the dot product of the result with the original vectors.

### 🟡 Intermediate
*Task*: Implement a **batch projection** function that takes a `(batch, dim)` tensor of vectors and a single base vector, returning the projected batch without explicit Python loops. Benchmark CPU vs GPU performance for `batch=10_000, dim=512`.

### 🔴 Production-Scale System
*Task*: Design a **streaming projection service**:
- Accepts a gRPC stream of high‑dimensional embeddings.
- Projects each embedding onto a learned basis (matrix of size `dim × k`).
- Uses TorchScript‑compiled kernels for low‑latency inference.
- Includes graceful back‑pressure handling, memory‑pool reuse, and observability (Prometheus metrics for latency & throughput).  
Document the architecture diagram, key data structures, and a minimal prototype showing end‑to‑end flow.
