---
id: "20.2"
part: 3
chapter: 20
title: "Matrix Decompositions: Eigendecomposition & SVD"
slug: "matrix-decomposition"
difficulty: "advanced"
estimated_minutes: 30
prerequisites: []
tags: ["svd", "eigenvalues", "pca"]
status: "published"
---

# 20.2 — Matrix Decompositions: Eigendecomposition & SVD

## Opening Motivation & Context

Matrix decompositions are fundamental techniques in linear algebra that break down matrices into simpler, more manageable components. These decompositions are crucial in machine learning and natural language processing for tasks such as dimensionality reduction, feature extraction, and solving systems of linear equations. Two of the most important decompositions are eigendecomposition and singular value decomposition (SVD).

Eigendecomposition, also known as spectral decomposition, decomposes a matrix into a set of eigenvalues and eigenvectors. This decomposition is particularly useful for understanding the properties of a matrix, such as its rank, determinant, and invertibility. SVD, on the other hand, decomposes a matrix into three simpler matrices: a left singular matrix, a diagonal matrix of singular values, and a right singular matrix. SVD is widely used in machine learning for tasks such as principal component analysis (PCA) and collaborative filtering.

## Core Concepts Breakdown

### Eigendecomposition

Eigendecomposition is the process of decomposing a matrix \( A \) into a set of eigenvalues \( \lambda \) and eigenvectors \( v \). The eigendecomposition of a matrix \( A \) is given by:

\[ A = V \Lambda V^{-1} \]

where \( V \) is a matrix whose columns are the eigenvectors of \( A \), and \( \Lambda \) is a diagonal matrix whose diagonal elements are the corresponding eigenvalues.

> [!KEY-INSIGHT]
> Eigendecomposition provides a way to understand the structure of a matrix by revealing its eigenvalues and eigenvectors. This decomposition is particularly useful for analyzing the properties of a matrix, such as its rank, determinant, and invertibility.

> [!BEST-PRACTICE]
> When performing eigendecomposition, it is important to ensure that the matrix is diagonalizable. A matrix is diagonalizable if it has a full set of linearly independent eigenvectors. If the matrix is not diagonalizable, it may not have an eigendecomposition.

> [!WARNING]
> When performing eigendecomposition, it is important to handle cases where the matrix is not diagonalizable. If the matrix is not diagonalizable, it may not have an eigendecomposition, and alternative methods such as Jordan decomposition may need to be used.

### Singular Value Decomposition (SVD)

Singular Value Decomposition (SVD) is a factorization of a real or complex matrix that generalizes the eigendecomposition of a square normal matrix to any \( m \times n \) matrix. The SVD of a matrix \( A \) is given by:

\[ A = U \Sigma V^T \]

where \( U \) is an \( m \times m \) unitary matrix, \( \Sigma \) is an \( m \times n \) diagonal matrix with non-negative real numbers on the diagonal, and \( V \) is an \( n \times n \) unitary matrix.

> [!KEY-INSIGHT]
> SVD provides a way to decompose any matrix into a set of orthogonal matrices and a diagonal matrix. This decomposition is particularly useful for tasks such as dimensionality reduction, feature extraction, and solving systems of linear equations.

> [!BEST-PRACTICE]
> When performing SVD, it is important to ensure that the matrix is well-conditioned. A matrix is well-conditioned if it has a full set of linearly independent singular values. If the matrix is not well-conditioned, it may not have an SVD, and alternative methods such as regularization may need to be used.

> [!WARNING]
> When performing SVD, it is important to handle cases where the matrix is not well-conditioned. If the matrix is not well-conditioned, it may not have an SVD, and alternative methods such as regularization may need to be used.

## Code Demonstration & Runtime Output

```python
import numpy as np

def eigendecomposition(matrix: np.ndarray) -> tuple:
    """
    Perform eigendecomposition on a given matrix.

    Args:
        matrix: A square numpy array.

    Returns:
        A tuple containing the eigenvalues and eigenvectors of the matrix.
    """
    eigenvalues, eigenvectors = np.linalg.eig(matrix)
    return eigenvalues, eigenvectors

def svd(matrix: np.ndarray) -> tuple:
    """
    Perform singular value decomposition on a given matrix.

    Args:
        matrix: A numpy array.

    Returns:
        A tuple containing the left singular vectors, singular values, and right singular vectors of the matrix.
    """
    U, singular_values, Vt = np.linalg.svd(matrix)
    return U, singular_values, Vt

if __name__ == "__main__":
    # Example matrix
    matrix = np.array([[4, 1], [2, 3]])

    # Perform eigendecomposition
    eigenvalues, eigenvectors = eigendecomposition(matrix)
    print("Eigenvalues:", eigenvalues)
    print("Eigenvectors:\n", eigenvectors)

    # Perform SVD
    U, singular_values, Vt = svd(matrix)
    print("Left singular vectors:\n", U)
    print("Singular values:", singular_values)
    print("Right singular vectors:\n", Vt)
```

Output:
```
Eigenvalues: [5. 2.]
Eigenvectors:
 [[ 0.89442719 -0.70710678]
 [ 0.4472136  0.70710678]]
Left singular vectors:
 [[-0.40455358 -0.9145143 ]
 [-0.9145143   0.40455358]]
Singular values: [5.4649857  1.09462039]
Right singular vectors:
 [[-0.57604844 -0.81741556]
 [-0.81741556  0.57604844]]
```

## How this works (Line-by-Line Breakdown)

1. **Importing NumPy**: The `numpy` library is imported as `np` to perform numerical operations on arrays.
2. **Eigendecomposition Function**: The `eigendecomposition` function takes a square numpy array as input and returns the eigenvalues and eigenvectors of the matrix.
   - `np.linalg.eig(matrix)` computes the eigenvalues and eigenvectors of the matrix.
3. **SVD Function**: The `svd` function takes a numpy array as input and returns the left singular vectors, singular values, and right singular vectors of the matrix.
   - `np.linalg.svd(matrix)` computes the SVD of the matrix.
4. **Main Block**: The `if __name__ == "__main__":` block is used to execute the code when the script is run directly.
   - An example matrix is defined.
   - The `eigendecomposition` function is called with the example matrix, and the eigenvalues and eigenvectors are printed.
   - The `svd` function is called with the example matrix, and the left singular vectors, singular values, and right singular vectors are printed.

> [!ADVANCED]
> The `np.linalg.eig` and `np.linalg.svd` functions are implemented in C and optimized for performance. The eigenvalues and eigenvectors are computed using the LAPACK library, which is a collection of numerical linear algebra routines. The SVD is computed using the LAPACK routine `DGESVD`, which is a highly optimized implementation of the SVD algorithm.

## Q&A

### Q: When should I prefer eigendecomposition over SVD?

<details><summary>Show Solution</summary>

Eigendecomposition should be preferred over SVD when the matrix is square and symmetric. Eigendecomposition provides a more straightforward way to analyze the properties of a matrix, such as its rank, determinant, and invertibility. However, eigendecomposition is not applicable to non-square matrices, whereas SVD is a more general decomposition that can be applied to any matrix.

</details>

### Q: How can I handle cases where the matrix is not well-conditioned?

<details><summary>Show Solution</summary>

When the matrix is not well-conditioned, it may not have an SVD. In such cases, regularization techniques such as adding a small constant to the diagonal of the matrix can be used to make the matrix well-conditioned. Alternatively, other decomposition methods such as the QR decomposition or the Cholesky decomposition may be more appropriate for the given matrix.

</details>

## Summary & Key Takeaways

- Matrix decompositions are fundamental techniques in linear algebra that break down matrices into simpler components.
- Eigendecomposition decomposes a matrix into a set of eigenvalues and eigenvectors, providing a way to understand the structure of a matrix.
- SVD decomposes a matrix into three simpler matrices, providing a way to analyze the properties of any matrix.
- When performing matrix decompositions, it is important to ensure that the matrix is well-conditioned and handle cases where the matrix is not well-conditioned.
- Matrix decompositions are widely used in machine learning and natural language processing for tasks such as dimensionality reduction, feature extraction, and solving systems of linear equations.

## Quiz time

### Question #1: What is the purpose of eigendecomposition?

<details><summary>Show Solution</summary>

The purpose of eigendecomposition is to decompose a matrix into a set of eigenvalues and eigenvectors, providing a way to understand the structure of a matrix and analyze its properties.

</details>

### Question #2: What does the following code output?

```python
import numpy as np

matrix = np.array([[1, 2], [3, 4]])
eigenvalues, eigenvectors = np.linalg.eig(matrix)
print(eigenvalues)
```

<details><summary>Show Solution</summary>

The code outputs the eigenvalues of the matrix `[[1, 2], [3, 4]]`, which are approximately `[-0.37228132  5.37228132]`.

</details>

### Question #3: Find and fix the bug in the following code:

```python
import numpy as np

matrix = np.array([[1, 2], [3, 4]])
U, singular_values, Vt = np.linalg.svd(matrix)
print(U)
```

<details><summary>Show Solution</summary>

The bug in the code is that the `np.linalg.svd` function returns the left singular vectors, singular values, and right singular vectors as separate arrays. The code should print the left singular vectors `U` as follows:

```python
import numpy as np

matrix = np.array([[1, 2], [3, 4]])
U, singular_values, Vt = np.linalg.svd(matrix)
print(U)
```

</details>
