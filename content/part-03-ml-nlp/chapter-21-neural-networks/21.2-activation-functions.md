---
id: "21.2"
part: 3
chapter: 21
title: "Activation Functions: ReLU, GeLU, SwiGLU & Vanishing Gradients"
slug: "activation-functions"
difficulty: "intermediate"
estimated_minutes: 25
prerequisites: []
tags: ["gelu", "swiglu", "activations"]
status: "published"
---

# 21.2 — Activation Functions: ReLU, GeLU, SwiGLU & Vanishing Gradients

## Opening Motivation & Context

Activation functions are a crucial component of neural networks, as they introduce non-linearity into the model, allowing it to learn complex patterns. Without activation functions, neural networks would essentially be linear models, limited in their ability to solve real-world problems. This lesson explores four popular activation functions: ReLU, GeLU, SwiGLU, and their relationship with vanishing gradients.

## Core Concepts Breakdown

### ReLU (Rectified Linear Unit)

ReLU is one of the most widely used activation functions in neural networks. It is defined as:

\[ \text{ReLU}(x) = \max(0, x) \]

> [!KEY-INSIGHT]
> ReLU is computationally efficient and helps mitigate the vanishing gradient problem by allowing gradients to flow unimpeded for positive inputs.

> [!BEST-PRACTICE]
> Use ReLU as the default activation function for hidden layers in feedforward neural networks.

> [!WARNING]
> ReLU can suffer from the "dying ReLU" problem, where neurons get stuck in the negative region and stop learning. Techniques like Leaky ReLU or Parametric ReLU can help address this issue.

### GeLU (Gaussian Error Linear Unit)

GeLU is a smooth approximation of ReLU, defined as:

\[ \text{GeLU}(x) = x \Phi(x) \]

where \(\Phi(x)\) is the cumulative distribution function of the standard Gaussian distribution.

> [!KEY-INSIGHT]
> GeLU is differentiable everywhere, which can lead to more stable training compared to ReLU.

> [!BEST-PRACTICE]
> Use GeLU in transformer architectures, as it has been shown to improve performance in natural language processing tasks.

### SwiGLU (Swish with Gated Linear Unit)

SwiGLU is a variant of the Swish activation function, defined as:

\[ \text{SwiGLU}(x, y) = x \cdot \sigma(y) \]

where \(\sigma\) is the sigmoid function.

> [!KEY-INSIGHT]
> SwiGLU combines the benefits of Swish and GLU, allowing for more expressive models while maintaining computational efficiency.

> [!BEST-PRACTICE]
> Use SwiGLU in transformer architectures, particularly in the feedforward layers, to improve model performance.

### Vanishing Gradients

Vanishing gradients occur when gradients become extremely small during backpropagation, causing the weights of early layers to update very slowly or not at all. This can lead to poor model performance.

> [!KEY-INSIGHT]
> Activation functions like ReLU and GeLU help mitigate the vanishing gradient problem by allowing gradients to flow unimpeded for positive inputs.

> [!BEST-PRACTICE]
> Use batch normalization or skip connections to further address the vanishing gradient problem.

## Code Demonstration & Runtime Output

```python
import numpy as np
import matplotlib.pyplot as plt
from scipy.special import erf

def relu(x: np.ndarray) -> np.ndarray:
    """Rectified Linear Unit activation function."""
    return np.maximum(0, x)

def gelu(x: np.ndarray) -> np.ndarray:
    """Gaussian Error Linear Unit activation function."""
    return x * 0.5 * (1 + erf(x / np.sqrt(2)))

def swish(x: np.ndarray) -> np.ndarray:
    """Swish activation function."""
    return x * (1 / (1 + np.exp(-x)))

def swiglu(x: np.ndarray, y: np.ndarray) -> np.ndarray:
    """Swish with Gated Linear Unit activation function."""
    return x * (1 / (1 + np.exp(-y)))

def plot_activation_functions():
    """Plot the activation functions."""
    x = np.linspace(-5, 5, 100)
    y = np.linspace(-5, 5, 100)
    X, Y = np.meshgrid(x, y)

    fig, axs = plt.subplots(2, 2, figsize=(10, 8))

    # ReLU
    axs[0, 0].plot(x, relu(x))
    axs[0, 0].set_title('ReLU')
    axs[0, 0].grid(True)

    # GeLU
    axs[0, 1].plot(x, gelu(x))
    axs[0, 1].set_title('GeLU')
    axs[0, 1].grid(True)

    # Swish
    axs[1, 0].plot(x, swish(x))
    axs[1, 0].set_title('Swish')
    axs[1, 0].grid(True)

    # SwiGLU
    Z = swiglu(X, Y)
    contour = axs[1, 1].contourf(X, Y, Z, levels=20, cmap='viridis')
    fig.colorbar(contour, ax=axs[1, 1])
    axs[1, 1].set_title('SwiGLU')
    axs[1, 1].grid(True)

    plt.tight_layout()
    plt.show()

if __name__ == '__main__':
    plot_activation_functions()
```

```text
Output:
A 2x2 grid of plots showing the ReLU, GeLU, Swish, and SwiGLU activation functions.
```

## How this works (Line-by-Line Breakdown)

1. **Imports**: The code imports necessary libraries, including NumPy for numerical operations, Matplotlib for plotting, and SciPy for the error function.
2. **Activation Functions**:
   - `relu`: Implements the ReLU activation function using NumPy's `maximum` function.
   - `gelu`: Implements the GeLU activation function using the error function from SciPy.
   - `swish`: Implements the Swish activation function using the sigmoid function.
   - `swiglu`: Implements the SwiGLU activation function by combining the Swish and GLU operations.
3. **Plotting Function**:
   - `plot_activation_functions`: Creates a 2x2 grid of plots to visualize the activation functions.
   - ReLU and GeLU are plotted as 1D functions.
   - Swish is plotted as a 1D function.
   - SwiGLU is plotted as a 2D contour plot to show its dependence on two inputs.
4. **Main Block**: Calls the `plot_activation_functions` function to generate and display the plots.

> [!ADVANCED]
> The `gelu` function uses the error function from SciPy, which is implemented in C for efficiency. The `swiglu` function combines the Swish and GLU operations, allowing for more expressive models while maintaining computational efficiency.

> [!QA]
> Q: When should I prefer GeLU over ReLU?
> A: GeLU is differentiable everywhere, which can lead to more stable training compared to ReLU. GeLU is particularly useful in transformer architectures, where it has been shown to improve performance in natural language processing tasks.

## Summary & Key Takeaways

- ReLU is a widely used activation function that introduces non-linearity and helps mitigate the vanishing gradient problem.
- GeLU is a smooth approximation of ReLU that is differentiable everywhere, making it suitable for transformer architectures.
- SwiGLU combines the benefits of Swish and GLU, allowing for more expressive models while maintaining computational efficiency.
- Vanishing gradients can be mitigated by using activation functions like ReLU and GeLU, as well as techniques like batch normalization and skip connections.

## Quiz time

### Question #1: Conceptual question

What is the primary advantage of using ReLU as an activation function in neural networks?

<details><summary>Show Solution</summary>

ReLU is computationally efficient and helps mitigate the vanishing gradient problem by allowing gradients to flow unimpeded for positive inputs.

</details>

### Question #2: What does this code output?

```python
import numpy as np

def relu(x: np.ndarray) -> np.ndarray:
    """Rectified Linear Unit activation function."""
    return np.maximum(0, x)

x = np.array([-2, -1, 0, 1, 2])
print(relu(x))
```

<details><summary>Show Solution</summary>

The code outputs `[0 0 0 1 2]`, which is the result of applying the ReLU activation function to the input array.

</details>

### Question #3: Find and fix the bug

```python
import numpy as np

def gelu(x: np.ndarray) -> np.ndarray:
    """Gaussian Error Linear Unit activation function."""
    return x * 0.5 * (1 + np.erf(x / np.sqrt(2)))

x = np.array([-2, -1, 0, 1, 2])
print(gelu(x))
```

<details><summary>Show Solution</summary>

The bug in the code is that it uses `np.erf` instead of `scipy.special.erf`. The fixed code is:

```python
import numpy as np
from scipy.special import erf

def gelu(x: np.ndarray) -> np.ndarray:
    """Gaussian Error Linear Unit activation function."""
    return x * 0.5 * (1 + erf(x / np.sqrt(2)))

x = np.array([-2, -1, 0, 1, 2])
print(gelu(x))
```

The fixed code outputs `[-0.04540157 -0.15865525  0.          0.84134475  1.95459843]`, which is the correct result of applying the GeLU activation function to the input array.

</details>
