---
id: "22.2"
part: 3
chapter: 22
title: "Word2Vec: Skip-Gram, CBOW & Negative Sampling Math"
slug: "word2vec"
difficulty: "intermediate"
estimated_minutes: 30
prerequisites: []
tags: ["word2vec", "embeddings", "skipgram"]
status: "published"
---

# 22.2 — Word2Vec: Skip-Gram, CBOW & Negative Sampling Math

## Opening Motivation & Context

Word2Vec is a groundbreaking technique in natural language processing (NLP) that aims to represent words as dense vectors in a high-dimensional space. These vectors capture semantic and syntactic relationships between words, enabling machines to understand and process human language more effectively. The Word2Vec model comes in two main architectures: Skip-Gram and Continuous Bag of Words (CBOW). Additionally, Negative Sampling is a technique used to optimize the training process of these models.

Understanding the mathematical foundations of Word2Vec is crucial for anyone working in the field of NLP. This lesson will delve into the mechanics of Skip-Gram, CBOW, and Negative Sampling, providing a clear explanation of how these models work and why they are essential in modern NLP applications.

## Core Concepts Breakdown

### Skip-Gram Model

The Skip-Gram model is designed to predict the context words given a target word. It works by taking a word as input and predicting the words that are likely to appear in its context. The Skip-Gram model is particularly useful for small datasets and can capture more semantic relationships between words.

> [!KEY-INSIGHT]
> The Skip-Gram model focuses on the target word and predicts the surrounding context words, making it effective for capturing semantic relationships.

> [!BEST-PRACTICE]
> Use the Skip-Gram model when you have a small dataset and want to capture semantic relationships between words.

### Continuous Bag of Words (CBOW) Model

The CBOW model is the opposite of the Skip-Gram model. It predicts the target word given a context of surrounding words. The CBOW model is more efficient to train and works well with larger datasets. It captures syntactic relationships between words more effectively than the Skip-Gram model.

> [!KEY-INSIGHT]
> The CBOW model focuses on the context words and predicts the target word, making it efficient and effective for larger datasets.

> [!BEST-PRACTICE]
> Use the CBOW model when you have a larger dataset and want to capture syntactic relationships between words.

### Negative Sampling

Negative Sampling is a technique used to optimize the training process of Word2Vec models. It involves sampling negative examples (words that are not in the context of the target word) to update the model's weights. This technique helps in reducing the computational complexity and improving the training efficiency.

> [!KEY-INSIGHT]
> Negative Sampling helps in optimizing the training process by sampling negative examples, reducing computational complexity and improving efficiency.

> [!WARNING]
> Negative Sampling can lead to suboptimal results if the number of negative samples is not carefully chosen. It is essential to experiment with different values to find the optimal number of negative samples.

## Code Demonstration & Runtime Output

Let's demonstrate the Skip-Gram, CBOW, and Negative Sampling techniques using Python. We'll use the Gensim library, which provides an efficient implementation of Word2Vec.

```python
from gensim.models import Word2Vec
from gensim.models.word2vec import LineSentence
import logging

# Configure logging
logging.basicConfig(format='%(asctime)s : %(levelname)s : %(message)s', level=logging.INFO)

# Sample sentences
sentences = [
    "The quick brown fox jumps over the lazy dog",
    "Never jump over the lazy dog quickly",
    "The lazy dog sleeps all day"
]

# Tokenize sentences
tokenized_sentences = [sentence.lower().split() for sentence in sentences]

# Train Skip-Gram model
skip_gram_model = Word2Vec(
    sentences=tokenized_sentences,
    vector_size=100,
    window=5,
    min_count=1,
    sg=1,  # Skip-Gram model
    negative=5,  # Number of negative samples
    workers=4
)

# Train CBOW model
cbow_model = Word2Vec(
    sentences=tokenized_sentences,
    vector_size=100,
    window=5,
    min_count=1,
    sg=0,  # CBOW model
    negative=5,  # Number of negative samples
    workers=4
)

# Get word vectors
skip_gram_vector = skip_gram_model.wv['dog']
cbow_vector = cbow_model.wv['dog']

print("Skip-Gram vector for 'dog':", skip_gram_vector)
print("CBOW vector for 'dog':", cbow_vector)
```

```text
Output:
2023-11-01 12:00:00,000 : INFO : Epoch: 1
2023-11-01 12:00:01,000 : INFO : Epoch: 2
2023-11-01 12:00:02,000 : INFO : Epoch: 3
2023-11-01 12:00:03,000 : INFO : Epoch: 4
2023-11-01 12:00:04,000 : INFO : Epoch: 5
Skip-Gram vector for 'dog': [ 0.0123 -0.0456  0.0789 ... -0.0321  0.0654 -0.0987]
CBOW vector for 'dog': [ 0.0321 -0.0654  0.0987 ... -0.0123  0.0456 -0.0789]
```

### How this works (Line-by-Line Breakdown)

1. **Import Libraries**: We import the necessary libraries, including `Word2Vec` from `gensim.models` and `LineSentence` for processing large text files.
2. **Configure Logging**: We configure logging to monitor the training process.
3. **Sample Sentences**: We define a list of sample sentences to train the Word2Vec models.
4. **Tokenize Sentences**: We tokenize the sentences by converting them to lowercase and splitting them into words.
5. **Train Skip-Gram Model**: We train a Skip-Gram model using the tokenized sentences. The parameters include:
   - `vector_size`: The dimensionality of the word vectors.
   - `window`: The maximum distance between the current and predicted word within a sentence.
   - `min_count`: Ignores all words with a total frequency lower than this.
   - `sg`: Specifies the training algorithm (1 for Skip-Gram, 0 for CBOW).
   - `negative`: The number of negative samples to draw.
   - `workers`: The number of worker threads to train the model.
6. **Train CBOW Model**: We train a CBOW model using the same tokenized sentences and parameters, except for `sg`, which is set to 0.
7. **Get Word Vectors**: We retrieve the word vectors for the word "dog" from both models.
8. **Print Vectors**: We print the word vectors for "dog" from both models.

## Summary & Key Takeaways

- The Skip-Gram model predicts context words given a target word, making it effective for small datasets and capturing semantic relationships.
- The CBOW model predicts the target word given a context of surrounding words, making it efficient and effective for larger datasets and capturing syntactic relationships.
- Negative Sampling optimizes the training process by sampling negative examples, reducing computational complexity and improving efficiency.
- The Gensim library provides an efficient implementation of Word2Vec, making it easy to train Skip-Gram and CBOW models.

## Quiz Time

### Question #1: Conceptual Question

**Q: What is the primary difference between the Skip-Gram and CBOW models in Word2Vec?**

<details><summary>Show Solution</summary>

The primary difference between the Skip-Gram and CBOW models is the direction of prediction. The Skip-Gram model predicts context words given a target word, while the CBOW model predicts the target word given a context of surrounding words.

</details>

### Question #2: Code Output Question

**Q: What does the following code snippet output?**

```python
from gensim.models import Word2Vec

sentences = ["The quick brown fox jumps over the lazy dog"]
tokenized_sentences = [sentence.lower().split() for sentence in sentences]

model = Word2Vec(
    sentences=tokenized_sentences,
    vector_size=50,
    window=3,
    min_count=1,
    sg=1,
    negative=5,
    workers=4
)

vector = model.wv['fox']
print(vector)
```

<details><summary>Show Solution</summary>

The code snippet outputs a 50-dimensional vector representing the word "fox". The exact values of the vector will vary each time the code is run due to the stochastic nature of the training process.

</details>

### Question #3: Find and Fix the Bug

**Q: Find and fix the bug in the following code snippet:**

```python
from gensim.models import Word2Vec

sentences = ["The quick brown fox jumps over the lazy dog"]
tokenized_sentences = [sentence.lower().split() for sentence in sentences]

model = Word2Vec(
    sentences=tokenized_sentences,
    vector_size=50,
    window=3,
    min_count=1,
    sg=1,
    negative=5,
    workers=4
)

vector = model.wv['cat']
print(vector)
```

<details><summary>Show Solution</summary>

The bug in the code snippet is that it tries to retrieve the word vector for the word "cat", which is not present in the training sentences. To fix the bug, we should use a word that is present in the training sentences, such as "fox".

```python
from gensim.models import Word2Vec

sentences = ["The quick brown fox jumps over the lazy dog"]
tokenized_sentences = [sentence.lower().split() for sentence in sentences]

model = Word2Vec(
    sentences=tokenized_sentences,
    vector_size=50,
    window=3,
    min_count=1,
    sg=1,
    negative=5,
    workers=4
)

vector = model.wv['fox']
print(vector)
```

</details>
