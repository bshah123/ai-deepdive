---
id: "24.4"
part: 4
chapter: 24
title: "ALiBi & Relative Positional Biases for Length Extrapolation"
slug: "alibi-relative-pos"
difficulty: "advanced"
estimated_minutes: 30
prerequisites: []
tags: ["alibi", "extrapolation", "transformers"]
status: "published"
---

# 24.4 — ALiBi & Relative Positional Biases for Length Extrapolation

## Opening Motivation & Context

In the realm of Large Language Models (LLMs), understanding the positional relationships between tokens is crucial for tasks like language translation, text summarization, and question answering. Traditional positional encodings, such as those used in the Transformer architecture, rely on fixed-size vectors that are added to the input embeddings. However, these encodings have limitations when it comes to handling sequences of varying lengths, especially those longer than those seen during training.

Absolute Length Interpolation (ALiBi) is a technique designed to address these limitations by incorporating relative positional biases directly into the attention mechanism. This approach allows the model to better handle sequences of varying lengths, including those longer than those seen during training, a capability known as length extrapolation.

## Core Concepts Breakdown

### [!KEY-INSIGHT]
ALiBi introduces relative positional biases into the attention mechanism, enabling the model to handle sequences of varying lengths, including those longer than those seen during training.

### [!BEST-PRACTICE]
When implementing ALiBi, it's important to carefully choose the slope parameter, which controls the rate at which the positional biases decay. A well-chosen slope can significantly improve the model's ability to handle varying sequence lengths.

### [!WARNING]
ALiBi can be computationally expensive, especially for long sequences. It's important to consider the trade-off between computational cost and the benefits of length extrapolation when implementing this technique.

## Code Demonstration & Runtime Output

```python
import torch
import torch.nn as nn
import torch.nn.functional as F

class ALiBiAttention(nn.Module):
    def __init__(self, num_heads: int, head_dim: int, max_seq_len: int, slope: float = 1.0):
        super().__init__()
        self.num_heads = num_heads
        self.head_dim = head_dim
        self.max_seq_len = max_seq_len
        self.slope = slope

        # Initialize the positional biases
        self.positional_biases = self._init_positional_biases()

    def _init_positional_biases(self) -> torch.Tensor:
        # Create a matrix of relative positions
        relative_positions = torch.arange(self.max_seq_len, dtype=torch.float32)
        relative_positions = relative_positions.unsqueeze(0) - relative_positions.unsqueeze(1)

        # Apply the slope to the relative positions
        positional_biases = -self.slope * relative_positions.abs()

        # Reshape the biases for multi-head attention
        positional_biases = positional_biases.unsqueeze(0).unsqueeze(0).expand(
            self.num_heads, self.max_seq_len, self.max_seq_len
        )

        return positional_biases

    def forward(self, query: torch.Tensor, key: torch.Tensor, value: torch.Tensor, mask: torch.Tensor = None) -> torch.Tensor:
        # Compute the attention scores
        attention_scores = torch.matmul(query, key.transpose(-2, -1)) / (self.head_dim ** 0.5)

        # Add the positional biases to the attention scores
        attention_scores = attention_scores + self.positional_biases[:, :, :attention_scores.size(-2), :attention_scores.size(-1)]

        # Apply the mask if provided
        if mask is not None:
            attention_scores = attention_scores.masked_fill(mask == 0, float('-inf'))

        # Compute the attention weights
        attention_weights = F.softmax(attention_scores, dim=-1)

        # Apply the attention weights to the values
        output = torch.matmul(attention_weights, value)

        return output

if __name__ == "__main__":
    # Initialize the ALiBi attention module
    num_heads = 8
    head_dim = 64
    max_seq_len = 128
    slope = 1.0
    alibi_attention = ALiBiAttention(num_heads, head_dim, max_seq_len, slope)

    # Create random input tensors
    batch_size = 2
    seq_len = 64
    query = torch.randn(batch_size, num_heads, seq_len, head_dim)
    key = torch.randn(batch_size, num_heads, seq_len, head_dim)
    value = torch.randn(batch_size, num_heads, seq_len, head_dim)

    # Compute the output
    output = alibi_attention(query, key, value)

    print(f"Output shape: {output.shape}")
```

```text
Output:
Output shape: torch.Size([2, 8, 64, 64])
```

## How this works (Line-by-Line Breakdown)

1. **Imports**: We import the necessary PyTorch modules for building the ALiBi attention mechanism.
2. **ALiBiAttention Class**: We define a custom `nn.Module` class for the ALiBi attention mechanism.
3. **Initialization**: In the `__init__` method, we initialize the number of attention heads, the dimension of each head, the maximum sequence length, and the slope parameter for the positional biases.
4. **Positional Biases Initialization**: The `_init_positional_biases` method creates a matrix of relative positions and applies the slope to these positions to create the positional biases. These biases are then reshaped for multi-head attention.
5. **Forward Pass**: The `forward` method computes the attention scores, adds the positional biases, applies the mask if provided, computes the attention weights, and applies these weights to the values to produce the output.
6. **Main Block**: In the `__main__` block, we initialize the ALiBi attention module, create random input tensors, and compute the output to demonstrate the functionality of the code.

## [!ADVANCED]

The ALiBi attention mechanism leverages PyTorch's automatic differentiation to compute gradients during the backward pass, enabling efficient training of the model. The positional biases are added to the attention scores before the softmax operation, which allows the model to incorporate positional information directly into the attention mechanism. This approach is computationally efficient and can be easily integrated into existing Transformer architectures.

## [!QA]

**Q: When should I prefer ALiBi over traditional positional encodings?**

A: ALiBi is particularly useful when you need to handle sequences of varying lengths, especially those longer than those seen during training. Traditional positional encodings are fixed-size and may not generalize well to sequences of different lengths. ALiBi's relative positional biases allow the model to better handle varying sequence lengths, making it a preferred choice for tasks requiring length extrapolation.

## Summary & Key Takeaways

- ALiBi introduces relative positional biases into the attention mechanism, enabling the model to handle sequences of varying lengths.
- The slope parameter controls the rate at which the positional biases decay and should be carefully chosen.
- ALiBi can be computationally expensive, especially for long sequences, and the trade-off between computational cost and the benefits of length extrapolation should be considered.
- The ALiBi attention mechanism can be easily integrated into existing Transformer architectures.

## Quiz time

### Question #1: Conceptual question

**What is the primary advantage of using ALiBi over traditional positional encodings?**

<details><summary>Show Solution</summary>

ALiBi's primary advantage is its ability to handle sequences of varying lengths, including those longer than those seen during training, through the use of relative positional biases.

</details>

### Question #2: What does this code output?

```python
import torch

# Create a matrix of relative positions
relative_positions = torch.arange(5, dtype=torch.float32)
relative_positions = relative_positions.unsqueeze(0) - relative_positions.unsqueeze(1)

print(relative_positions)
```

<details><summary>Show Solution</summary>

The code outputs a matrix of relative positions:

```
tensor([[-0., -1., -2., -3., -4.],
        [ 1., -0., -1., -2., -3.],
        [ 2.,  1., -0., -1., -2.],
        [ 3.,  2.,  1., -0., -1.],
        [ 4.,  3.,  2.,  1., -0.]])
```

</details>

### Question #3: Find and fix the bug

```python
import torch
import torch.nn as nn
import torch.nn.functional as F

class ALiBiAttention(nn.Module):
    def __init__(self, num_heads: int, head_dim: int, max_seq_len: int, slope: float = 1.0):
        super().__init__()
        self.num_heads = num_heads
        self.head_dim = head_dim
        self.max_seq_len = max_seq_len
        self.slope = slope

        # Initialize the positional biases
        self.positional_biases = self._init_positional_biases()

    def _init_positional_biases(self) -> torch.Tensor:
        # Create a matrix of relative positions
        relative_positions = torch.arange(self.max_seq_len, dtype=torch.float32)
        relative_positions = relative_positions.unsqueeze(0) - relative_positions.unsqueeze(1)

        # Apply the slope to the relative positions
        positional_biases = -self.slope * relative_positions.abs()

        # Reshape the biases for multi-head attention
        positional_biases = positional_biases.unsqueeze(0).unsqueeze(0).expand(
            self.num_heads, self.max_seq_len, self.max_seq_len
        )

        return positional_biases

    def forward(self, query: torch.Tensor, key: torch.Tensor, value: torch.Tensor, mask: torch.Tensor = None) -> torch.Tensor:
        # Compute the attention scores
        attention_scores = torch.matmul(query, key.transpose(-2, -1)) / (self.head_dim ** 0.5)

        # Add the positional biases to the attention scores
        attention_scores = attention_scores + self.positional_biases[:, :, :attention_scores.size(-2), :attention_scores.size(-1)]

        # Apply the mask if provided
        if mask is not None:
            attention_scores = attention_scores.masked_fill(mask == 0, float('-inf'))

        # Compute the attention weights
        attention_weights = F.softmax(attention_scores, dim=-1)

        # Apply the attention weights to the values
        output = torch.matmul(attention_weights, value)

        return output

if __name__ == "__main__":
    # Initialize the ALiBi attention module
    num_heads = 8
    head_dim = 64
    max_seq_len = 128
    slope = 1.0
    alibi_attention = ALiBiAttention(num_heads, head_dim, max_seq_len, slope)

    # Create random input tensors
    batch_size = 2
    seq_len = 64
    query = torch.randn(batch_size, num_heads, seq_len, head_dim)
    key = torch.randn(batch_size, num_heads, seq_len, head_dim)
    value = torch.randn(batch_size, num_heads, seq_len, head_dim)

    # Compute the output
    output = alibi_attention(query, key, value)

    print(f"Output shape: {output.shape}")
```

The bug in this code is that the positional biases are not being properly sliced to match the sequence length of the input tensors. This can lead to incorrect attention scores and suboptimal performance.

Here is the fixed code:

```python
import torch
import torch.nn as nn
import torch.nn.functional as F

class ALiBiAttention(nn.Module):
    def __init__(self, num_heads: int, head_dim: int, max_seq_len: int, slope: float = 1.0):
        super().__init__()
        self.num_heads = num_heads
        self.head_dim = head_dim
        self.max_seq_len = max_seq_len
        self.slope = slope

        # Initialize the positional biases
        self.positional_biases = self._init_positional_biases()

    def _init_positional_biases(self) -> torch.Tensor:
        # Create a matrix of relative positions
        relative_positions = torch.arange(self.max_seq_len, dtype=torch.float32)
        relative_positions = relative_positions.unsqueeze(0) - relative_positions.unsqueeze(1)

        # Apply the slope to the relative positions
        positional_biases = -self.slope * relative_positions.abs()

        # Reshape the biases for multi-head attention
        positional_biases = positional_biases.unsqueeze(0).unsqueeze(0).expand(
            self.num_heads, self.max_seq_len, self.max_seq_len
        )

        return positional_biases

    def forward(self, query: torch.Tensor, key: torch.Tensor, value: torch.Tensor, mask: torch.Tensor = None) -> torch.Tensor:
        # Compute the attention scores
        attention_scores = torch.matmul(query, key.transpose(-2, -1)) / (self.head_dim ** 0.5)

        # Add the positional biases to the attention scores
        attention_scores = attention_scores + self.positional_biases[:, :, :attention_scores.size(-2), :attention_scores.size(-1)]

        # Apply the mask if provided
        if mask is not None:
            attention_scores = attention_scores.masked_fill(mask == 0, float('-inf'))

        # Compute the attention weights
        attention_weights = F.softmax(attention_scores, dim=-1)

        # Apply the attention weights to the values
        output = torch.matmul(attention_weights, value)

        return output

if __name__ == "__main__":
    # Initialize the ALiBi attention module
    num_heads = 8
    head_dim = 64
    max_seq_len = 128
    slope = 1.0
    alibi_attention = ALiBiAttention(num_heads, head_dim, max_seq_len, slope)

    # Create random input tensors
    batch_size = 2
    seq_len = 64
    query = torch.randn(batch_size, num_heads, seq_len, head_dim)
    key = torch.randn(batch_size, num_heads, seq_len, head_dim)
    value = torch.randn(batch_size, num_heads, seq_len, head_dim)

    # Compute the output
    output = alibi_attention(query, key, value)

    print(f"Output shape: {output.shape}")
```

The fix involves properly slicing the positional biases to match the sequence length of the input tensors in the `forward` method. This ensures that the attention scores are computed correctly and the model can handle sequences of varying lengths effectively.
