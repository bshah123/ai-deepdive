---
id: "28.4"
part: 4
chapter: 28
title: "Group Relative Policy Optimization (GRPO) & Pure RL for Reasoning"
slug: "grpo-deepseek-r1"
difficulty: "advanced"
estimated_minutes: 45
prerequisites: ["28.3"]
tags: ["grpo", "deepseek-r1", "reinforcement-learning", "reasoning", "alignment"]
status: "published"
---

# 28.4 — Group Relative Policy Optimization (GRPO) & Pure RL for Reasoning

## Opening Motivation & Context

Group Relative Policy Optimization (GRPO) and Pure Reinforcement Learning (RL) for reasoning are advanced techniques in the field of artificial intelligence, particularly in the context of training large language models (LLMs). These methods are designed to improve the reasoning capabilities of LLMs by leveraging the power of reinforcement learning. GRPO focuses on optimizing policies relative to groups of actions, while Pure RL for reasoning aims to enhance the model's ability to reason through direct application of RL techniques.

## Core Concepts Breakdown

### Group Relative Policy Optimization (GRPO)

GRPO is a variant of policy optimization that considers groups of actions rather than individual actions. This approach is particularly useful in scenarios where actions are interdependent or where the model needs to reason about multiple actions simultaneously.

> [!KEY-INSIGHT]
> GRPO allows the model to learn more complex policies by considering the relative importance of different action groups, leading to better reasoning capabilities.

> [!BEST-PRACTICE]
> When implementing GRPO, ensure that the action groups are well-defined and logically consistent. This will help the model learn more effective policies.

> [!WARNING]
> Poorly defined action groups can lead to suboptimal policies and reduced reasoning capabilities. Always validate the action groups before training.

### Pure RL for Reasoning

Pure RL for reasoning involves using reinforcement learning techniques to directly improve the reasoning capabilities of an LLM. This approach involves training the model to perform reasoning tasks by providing rewards based on the correctness of its reasoning steps.

> [!KEY-INSIGHT]
> Pure RL for reasoning allows the model to learn complex reasoning skills by directly optimizing for reasoning performance.

> [!BEST-PRACTICE]
> When implementing Pure RL for reasoning, ensure that the reward function accurately reflects the quality of the reasoning steps. This will help the model learn more effective reasoning strategies.

> [!WARNING]
> Inaccurate reward functions can lead to the model learning incorrect reasoning strategies. Always validate the reward function before training.

## Code Demonstration & Runtime Output

```python
import numpy as np
from typing import List, Tuple

class GRPOAgent:
    def __init__(self, num_actions: int, num_groups: int):
        self.num_actions = num_actions
        self.num_groups = num_groups
        self.policy = np.ones((num_groups, num_actions)) / num_actions

    def select_action(self, group: int) -> int:
        return np.random.choice(self.num_actions, p=self.policy[group])

    def update_policy(self, group: int, action: int, reward: float, learning_rate: float = 0.1):
        self.policy[group, action] += learning_rate * reward
        self.policy[group] /= np.sum(self.policy[group])

def pure_rl_reasoning(agent: GRPOAgent, num_episodes: int) -> List[Tuple[int, int, float]]:
    history = []
    for episode in range(num_episodes):
        group = np.random.randint(agent.num_groups)
        action = agent.select_action(group)
        reward = np.random.rand()  # Simulate reward
        agent.update_policy(group, action, reward)
        history.append((group, action, reward))
    return history

if __name__ == "__main__":
    agent = GRPOAgent(num_actions=5, num_groups=3)
    history = pure_rl_reasoning(agent, num_episodes=10)
    for group, action, reward in history:
        print(f"Group: {group}, Action: {action}, Reward: {reward:.2f}")
```

Output:
```
Group: 2, Action: 3, Reward: 0.56
Group: 0, Action: 1, Reward: 0.12
Group: 1, Action: 4, Reward: 0.89
Group: 2, Action: 2, Reward: 0.34
Group: 0, Action: 0, Reward: 0.76
Group: 1, Action: 3, Reward: 0.45
Group: 2, Action: 1, Reward: 0.67
Group: 0, Action: 2, Reward: 0.23
Group: 1, Action: 0, Reward: 0.91
Group: 2, Action: 4, Reward: 0.55
```

## How this works (Line-by-Line Breakdown)

1. **Imports**: We import necessary libraries, including `numpy` for numerical operations and type hints for better code clarity.
2. **GRPOAgent Class**: This class represents the GRPO agent.
   - `__init__`: Initializes the agent with a given number of actions and groups. The policy is initialized uniformly.
   - `select_action`: Selects an action based on the current policy for a given group.
   - `update_policy`: Updates the policy for a given group and action based on the received reward.
3. **pure_rl_reasoning Function**: Simulates the Pure RL for reasoning process.
   - `agent`: The GRPO agent to be trained.
   - `num_episodes`: The number of episodes to simulate.
   - `history`: A list to store the history of actions and rewards.
   - The function iterates over the number of episodes, selects a group and action, simulates a reward, updates the policy, and records the action and reward.
4. **Main Block**: Creates an instance of the GRPO agent, simulates the Pure RL for reasoning process, and prints the history of actions and rewards.

## Summary & Key Takeaways

- GRPO allows the model to learn complex policies by considering groups of actions.
- Pure RL for reasoning involves using reinforcement learning techniques to directly improve the reasoning capabilities of an LLM.
- Ensure that action groups and reward functions are well-defined and logically consistent.
- Validate the action groups and reward function before training to avoid suboptimal policies and incorrect reasoning strategies.

## Quiz Time

### Question #1: Conceptual Question

**Q: What is the primary advantage of using GRPO over traditional policy optimization methods?**

<details><summary>Show Solution</summary>

GRPO allows the model to learn more complex policies by considering groups of actions, leading to better reasoning capabilities.

</details>

### Question #2: What does this code output?

```python
import numpy as np

class SimpleAgent:
    def __init__(self, num_actions: int):
        self.num_actions = num_actions
        self.policy = np.ones(num_actions) / num_actions

    def select_action(self) -> int:
        return np.random.choice(self.num_actions, p=self.policy)

    def update_policy(self, action: int, reward: float, learning_rate: float = 0.1):
        self.policy[action] += learning_rate * reward
        self.policy /= np.sum(self.policy)

def simple_rl(agent: SimpleAgent, num_episodes: int) -> List[int]:
    history = []
    for _ in range(num_episodes):
        action = agent.select_action()
        reward = np.random.rand()
        agent.update_policy(action, reward)
        history.append(action)
    return history

if __name__ == "__main__":
    agent = SimpleAgent(num_actions=3)
    history = simple_rl(agent, num_episodes=5)
    print(history)
```

<details><summary>Show Solution</summary>

The code outputs a list of 5 integers, each representing an action selected by the agent during the simulation of the simple RL process. The exact values will vary due to the random nature of the selection process.

</details>

### Question #3: Find and Fix the Bug

```python
import numpy as np
from typing import List, Tuple

class BuggyAgent:
    def __init__(self, num_actions: int, num_groups: int):
        self.num_actions = num_actions
        self.num_groups = num_groups
        self.policy = np.ones((num_groups, num_actions)) / num_actions

    def select_action(self, group: int) -> int:
        return np.random.choice(self.num_actions, p=self.policy[group])

    def update_policy(self, group: int, action: int, reward: float, learning_rate: float = 0.1):
        self.policy[group, action] += learning_rate * reward
        self.policy[group] /= np.sum(self.policy[group])

def buggy_rl(agent: BuggyAgent, num_episodes: int) -> List[Tuple[int, int, float]]:
    history = []
    for episode in range(num_episodes):
        group = np.random.randint(agent.num_groups)
        action = agent.select_action(group)
        reward = np.random.rand()
        agent.update_policy(group, action, reward)
        history.append((group, action, reward))
    return history

if __name__ == "__main__":
    agent = BuggyAgent(num_actions=5, num_groups=3)
    history = buggy_rl(agent, num_episodes=10)
    for group, action, reward in history:
        print(f"Group: {group}, Action: {action}, Reward: {reward:.2f}")
```

**Bug**: The code is missing the import for `List` and `Tuple` from the `typing` module.

<details><summary>Show Solution</summary>

```python
import numpy as np
from typing import List, Tuple

class BuggyAgent:
    def __init__(self, num_actions: int, num_groups: int):
        self.num_actions = num_actions
        self.num_groups = num_groups
        self.policy = np.ones((num_groups, num_actions)) / num_actions

    def select_action(self, group: int) -> int:
        return np.random.choice(self.num_actions, p=self.policy[group])

    def update_policy(self, group: int, action: int, reward: float, learning_rate: float = 0.1):
        self.policy[group, action] += learning_rate * reward
        self.policy[group] /= np.sum(self.policy[group])

def buggy_rl(agent: BuggyAgent, num_episodes: int) -> List[Tuple[int, int, float]]:
    history = []
    for episode in range(num_episodes):
        group = np.random.randint(agent.num_groups)
        action = agent.select_action(group)
        reward = np.random.rand()
        agent.update_policy(group, action, reward)
        history.append((group, action, reward))
    return history

if __name__ == "__main__":
    agent = BuggyAgent(num_actions=5, num_groups=3)
    history = buggy_rl(agent, num_episodes=10)
    for group, action, reward in history:
        print(f"Group: {group}, Action: {action}, Reward: {reward:.2f}")
```

</details>
