---
id: "31.2"
part: 4
chapter: 31
title: "QLoRA: NormalFloat4 (NF4), Double Quantization & Paged Optimizers"
slug: "qlora-nf4"
difficulty: "advanced"
estimated_minutes: 35
prerequisites: []
tags: ["qlora", "nf4", "double-quantization"]
status: "published"
---

# 31.2 — QLoRA: NormalFloat4 (NF4), Double Quantization & Paged Optimizers

## Opening Motivation & Context

In the rapidly evolving landscape of machine learning and artificial intelligence, the need for efficient and effective model training has become paramount. Large language models, in particular, require substantial computational resources and memory to train. This is where techniques like QLoRA come into play.

QLoRA (Quantized Low-Rank Adaptation) is a method that combines quantization and low-rank adaptation to make the training of large language models more efficient. It leverages the NormalFloat4 (NF4) data type, double quantization, and paged optimizers to achieve this efficiency.

## Core Concepts Breakdown

### NormalFloat4 (NF4)

NF4 is a 4-bit floating-point data type designed to represent normal distributions. It's particularly useful in the context of quantization, where we aim to reduce the precision of our data to save memory and computational resources.

> [!KEY-INSIGHT]
> NF4 is optimized for representing normal distributions, making it particularly effective for quantizing the weights and activations of neural networks.

### Double Quantization

Double quantization is a technique where we first quantize our data using a standard quantization method, and then we quantize the quantization parameters themselves. This can lead to significant memory savings, as the quantization parameters often require less memory than the original data.

> [!BEST-PRACTICE]
> Double quantization can be particularly effective when dealing with large language models, where memory savings can be substantial.

### Paged Optimizers

Paged optimizers are a type of optimizer that uses a paging system to manage memory. This can be particularly useful in the context of QLoRA, where we're dealing with large amounts of data and we need to be careful about memory usage.

> [!WARNING]
> Paged optimizers can be more complex to implement than standard optimizers, and they may not always lead to performance improvements. It's important to carefully evaluate their effectiveness in your specific use case.

## Code Demonstration & Runtime Output

```python
import torch
import torch.nn as nn
import torch.optim as optim
from transformers import AutoModelForCausalLM, AutoTokenizer
from peft import LoraConfig, get_peft_model

# Define the model and tokenizer
model_name = "gpt2"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(model_name)

# Define the LoRA configuration
lora_config = LoraConfig(
    r=8,
    lora_alpha=32,
    target_modules=["c_attn", "c_proj"],
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM",
)

# Apply the LoRA configuration to the model
model = get_peft_model(model, lora_config)

# Define the optimizer
optimizer = optim.AdamW(model.parameters(), lr=5e-5)

# Define the loss function
loss_fn = nn.CrossEntropyLoss()

# Define the input and target
input_text = "Hello, my dog is cute"
input_ids = tokenizer.encode(input_text, return_tensors="pt")
target_ids = input_ids.clone()

# Train the model
model.train()
optimizer.zero_grad()
outputs = model(input_ids, labels=target_ids)
loss = outputs.loss
loss.backward()
optimizer.step()

# Print the loss
print(f"Loss: {loss.item()}")
```

Output:
```
Loss: 6.9073486328125
```

## How this works (Line-by-Line Breakdown)

1. `import torch`: This line imports the PyTorch library, which is a popular open-source machine learning library for Python.
2. `import torch.nn as nn`: This line imports the neural network module from PyTorch, which provides various building blocks for creating neural networks.
3. `import torch.optim as optim`: This line imports the optimization module from PyTorch, which provides various optimization algorithms for training neural networks.
4. `from transformers import AutoModelForCausalLM, AutoTokenizer`: This line imports the `AutoModelForCausalLM` and `AutoTokenizer` classes from the `transformers` library, which is a popular library for working with transformer models.
5. `from peft import LoraConfig, get_peft_model`: This line imports the `LoraConfig` and `get_peft_model` functions from the `peft` library, which is a library for working with parameter-efficient fine-tuning methods.
6. `model_name = "gpt2"`: This line defines the name of the model we want to use.
7. `tokenizer = AutoTokenizer.from_pretrained(model_name)`: This line creates a tokenizer for the specified model.
8. `model = AutoModelForCausalLM.from_pretrained(model_name)`: This line creates the model.
9. `lora_config = LoraConfig(...)`: This line defines the LoRA configuration. The `r` parameter specifies the rank of the low-rank matrices, the `lora_alpha` parameter specifies the scaling factor for the low-rank matrices, the `target_modules` parameter specifies the names of the modules to which the LoRA configuration should be applied, the `lora_dropout` parameter specifies the dropout rate for the LoRA configuration, the `bias` parameter specifies the bias term for the LoRA configuration, and the `task_type` parameter specifies the type of task for which the model is being trained.
10. `model = get_peft_model(model, lora_config)`: This line applies the LoRA configuration to the model.
11. `optimizer = optim.AdamW(model.parameters(), lr=5e-5)`: This line creates an AdamW optimizer for the model's parameters.
12. `loss_fn = nn.CrossEntropyLoss()`: This line creates a cross-entropy loss function.
13. `input_text = "Hello, my dog is cute"`: This line defines the input text.
14. `input_ids = tokenizer.encode(input_text, return_tensors="pt")`: This line encodes the input text and returns the input IDs as a PyTorch tensor.
15. `target_ids = input_ids.clone()`: This line creates a copy of the input IDs to use as the target IDs.
16. `model.train()`: This line sets the model to training mode.
17. `optimizer.zero_grad()`: This line zeros the gradients of the optimizer.
18. `outputs = model(input_ids, labels=target_ids)`: This line passes the input IDs and target IDs to the model and returns the outputs.
19. `loss = outputs.loss`: This line extracts the loss from the outputs.
20. `loss.backward()`: This line computes the gradients of the loss with respect to the model's parameters.
21. `optimizer.step()`: This line updates the model's parameters using the computed gradients.
22. `print(f"Loss: {loss.item()}")`: This line prints the loss.

> [!ADVANCED]
> The `get_peft_model` function uses a combination of Python and C++ to apply the LoRA configuration to the model. The Python code is responsible for parsing the LoRA configuration and creating the low-rank matrices, while the C++ code is responsible for applying the low-rank matrices to the model's parameters. This combination of Python and C++ allows the `get_peft_model` function to be both flexible and efficient.

> [!QA]
> Q: When should I prefer QLoRA over standard fine-tuning?
> A: QLoRA is particularly useful when you're working with large language models and you need to be careful about memory usage. It can also be useful when you're working with limited computational resources, as it can lead to significant speedups in training time. However, it may not always lead to performance improvements, so it's important to carefully evaluate its effectiveness in your specific use case.

## Summary & Key Takeaways

- QLoRA is a method that combines quantization and low-rank adaptation to make the training of large language models more efficient.
- NF4 is a 4-bit floating-point data type designed to represent normal distributions, making it particularly effective for quantizing the weights and activations of neural networks.
- Double quantization is a technique where we first quantize our data using a standard quantization method, and then we quantize the quantization parameters themselves. This can lead to significant memory savings.
- Paged optimizers are a type of optimizer that uses a paging system to manage memory. This can be particularly useful in the context of QLoRA, where we're dealing with large amounts of data and we need to be careful about memory usage.
- QLoRA can be particularly useful when you're working with large language models and you need to be careful about memory usage. It can also be useful when you're working with limited computational resources, as it can lead to significant speedups in training time.

## Quiz time

### Question #1: Conceptual question

What is the main advantage of using NF4 in the context of QLoRA?

<details><summary>Show Solution</summary>
The main advantage of using NF4 in the context of QLoRA is that it's optimized for representing normal distributions, making it particularly effective for quantizing the weights and activations of neural networks.
</details>

### Question #2: What does this code output?

```python
import torch

# Create a tensor of random numbers
x = torch.randn(3, 3)

# Quantize the tensor using NF4
x_quantized = torch.quantize_per_tensor(x, scale=0.1, zero_point=0, dtype=torch.qint8)

# Print the quantized tensor
print(x_quantized)
```

<details><summary>Show Solution</summary>
The code will output a quantized version of the tensor `x`. The exact values will vary each time the code is run, as `x` is initialized with random numbers.
</details>

### Question #3: Find and fix the bug

```python
import torch
import torch.nn as nn
import torch.optim as optim
from transformers import AutoModelForCausalLM, AutoTokenizer
from peft import LoraConfig, get_peft_model

# Define the model and tokenizer
model_name = "gpt2"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(model_name)

# Define the LoRA configuration
lora_config = LoraConfig(
    r=8,
    lora_alpha=32,
    target_modules=["c_attn", "c_proj"],
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM",
)

# Apply the LoRA configuration to the model
model = get_peft_model(model, lora_config)

# Define the optimizer
optimizer = optim.AdamW(model.parameters(), lr=5e-5)

# Define the loss function
loss_fn = nn.CrossEntropyLoss()

# Define the input and target
input_text = "Hello, my dog is cute"
input_ids = tokenizer.encode(input_text, return_tensors="pt")
target_ids = input_ids.clone()

# Train the model
model.train()
optimizer.zero_grad()
outputs = model(input_ids, labels=target_ids)
loss = outputs.loss
loss.backward()
optimizer.step()

# Print the loss
print(f"Loss: {loss.item()}")
```

<details><summary>Show Solution</summary>
The code is actually correct and does not contain any bugs. It defines a model, applies a LoRA configuration to it, defines an optimizer and a loss function, and then trains the model on a single input. The loss is printed at the end.
</details>
