---
id: "47.1"
part: 7
chapter: 47
title: "JSON Schema Specification for Function Calling"
slug: "json-schema-tools"
difficulty: "beginner"
estimated_minutes: 20
prerequisites: []
tags: ["json-schema", "tools", "openai"]
status: "published"
---

# 47.1 — JSON Schema Specification for Function Calling

## Opening Motivation & Context

In the realm of Large Language Models (LLMs), function calling is a powerful feature that allows LLMs to interact with external tools and APIs. This interaction is facilitated by a structured communication protocol, where the LLM's output is formatted according to a JSON schema. This schema defines the structure and data types of the information that the LLM needs to provide to the external tool.

Understanding JSON schema specification for function calling is crucial for developers who want to build applications that leverage the full potential of LLMs. It enables the creation of robust, reliable, and maintainable systems that can handle complex tasks and interactions.

## Core Concepts Breakdown

### JSON Schema Basics

JSON Schema is a vocabulary that allows you to annotate and validate JSON documents. It provides a way to define the structure, data types, and constraints of JSON data. In the context of function calling, JSON Schema is used to define the structure of the LLM's output.

> [!KEY-INSIGHT]
> JSON Schema acts as a contract between the LLM and the external tool, ensuring that the data exchanged is structured and validated.

### Schema for Function Calling

When using function calling, the LLM's output is typically a JSON object that contains the necessary information for the external tool to perform its task. The JSON Schema for function calling defines the structure of this JSON object, including the required fields, their data types, and any constraints.

> [!BEST-PRACTICE]
> Always define a JSON Schema for your function calling implementation. This ensures that the LLM's output is structured and validated, making it easier to parse and use.

### Validation

Once the JSON Schema is defined, it can be used to validate the LLM's output. This validation ensures that the output conforms to the expected structure and data types, preventing errors and inconsistencies.

> [!WARNING]
> Without validation, the LLM's output may not conform to the expected structure, leading to errors and inconsistencies. Always validate the LLM's output against the JSON Schema.

## Code Demonstration & Runtime Output

```python
import json
from jsonschema import validate, ValidationError

# Define the JSON Schema for function calling
function_schema = {
    "type": "object",
    "properties": {
        "function_name": {"type": "string"},
        "arguments": {
            "type": "object",
            "properties": {
                "arg1": {"type": "string"},
                "arg2": {"type": "number"}
            },
            "required": ["arg1", "arg2"]
        }
    },
    "required": ["function_name", "arguments"]
}

# Example LLM output
llm_output = {
    "function_name": "example_function",
    "arguments": {
        "arg1": "example_value",
        "arg2": 42
    }
}

# Validate the LLM output against the schema
try:
    validate(instance=llm_output, schema=function_schema)
    print("Validation successful. The LLM output is valid.")
except ValidationError as e:
    print(f"Validation failed. Error: {e}")

# Convert the LLM output to a JSON string
json_output = json.dumps(llm_output, indent=2)
print("\nJSON Output:")
print(json_output)
```

```text
Output:
Validation successful. The LLM output is valid.

JSON Output:
{
  "function_name": "example_function",
  "arguments": {
    "arg1": "example_value",
    "arg2": 42
  }
}
```

## How this works (Line-by-Line Breakdown)

1. **Import Statements**: The code starts by importing the necessary modules, `json` for JSON operations and `jsonschema` for schema validation.
2. **Schema Definition**: The `function_schema` variable is defined as a dictionary that represents the JSON Schema for function calling. It specifies the structure, data types, and constraints of the LLM's output.
3. **LLM Output**: The `llm_output` variable is defined as a dictionary that represents the LLM's output. It contains the necessary information for the external tool to perform its task.
4. **Validation**: The `validate` function from the `jsonschema` module is used to validate the LLM's output against the schema. If the validation is successful, a success message is printed. If not, an error message is printed.
5. **JSON Conversion**: The `json.dumps` function is used to convert the LLM's output to a JSON string. The resulting JSON string is then printed.

## Summary & Key Takeaways

- JSON Schema is a powerful tool for defining and validating the structure of JSON data.
- In the context of function calling, JSON Schema is used to define the structure of the LLM's output.
- Validation ensures that the LLM's output conforms to the expected structure and data types.
- Always define a JSON Schema for your function calling implementation.
- Always validate the LLM's output against the JSON Schema.

## Quiz time

### Question #1: Conceptual question

What is the purpose of JSON Schema in the context of function calling?

<details><summary>Show Solution</summary>

JSON Schema is used to define the structure, data types, and constraints of the LLM's output. It acts as a contract between the LLM and the external tool, ensuring that the data exchanged is structured and validated.

</details>

### Question #2: What does this code output?

```python
import json
from jsonschema import validate, ValidationError

function_schema = {
    "type": "object",
    "properties": {
        "function_name": {"type": "string"},
        "arguments": {
            "type": "object",
            "properties": {
                "arg1": {"type": "string"},
                "arg2": {"type": "number"}
            },
            "required": ["arg1", "arg2"]
        }
    },
    "required": ["function_name", "arguments"]
}

llm_output = {
    "function_name": "example_function",
    "arguments": {
        "arg1": "example_value",
        "arg2": "not_a_number"
    }
}

try:
    validate(instance=llm_output, schema=function_schema)
    print("Validation successful. The LLM output is valid.")
except ValidationError as e:
    print(f"Validation failed. Error: {e}")
```

<details><summary>Show Solution</summary>

The code outputs:

```text
Validation failed. Error: 'not_a_number' is not of type 'number'
```

This is because the value of `arg2` in the LLM's output is a string, but the schema specifies that it should be a number.

</details>

### Question #3: Find and fix the bug

```python
import json
from jsonschema import validate, ValidationError

function_schema = {
    "type": "object",
    "properties": {
        "function_name": {"type": "string"},
        "arguments": {
            "type": "object",
            "properties": {
                "arg1": {"type": "string"},
                "arg2": {"type": "number"}
            },
            "required": ["arg1"]
        }
    },
    "required": ["function_name"]
}

llm_output = {
    "function_name": "example_function",
    "arguments": {
        "arg1": "example_value",
        "arg2": 42
    }
}

try:
    validate(instance=llm_output, schema=function_schema)
    print("Validation successful. The LLM output is valid.")
except ValidationError as e:
    print(f"Validation failed. Error: {e}")
```

<details><summary>Show Solution</summary>

The bug in the code is that the `required` field in the `arguments` object of the schema is set to `["arg1"]`, but it should be set to `["arg1", "arg2"]` to match the LLM's output.

Here is the fixed code:

```python
import json
from jsonschema import validate, ValidationError

function_schema = {
    "type": "object",
    "properties": {
        "function_name": {"type": "string"},
        "arguments": {
            "type": "object",
            "properties": {
                "arg1": {"type": "string"},
                "arg2": {"type": "number"}
            },
            "required": ["arg1", "arg2"]
        }
    },
    "required": ["function_name", "arguments"]
}

llm_output = {
    "function_name": "example_function",
    "arguments": {
        "arg1": "example_value",
        "arg2": 42
    }
}

try:
    validate(instance=llm_output, schema=function_schema)
    print("Validation successful. The LLM output is valid.")
except ValidationError as e:
    print(f"Validation failed. Error: {e}")
```

</details>
