---
id: "49.2"
part: 8
chapter: 49
title: "Long-Term Episodic & Semantic Memory with Vector Stores"
slug: "long-term-memory"
difficulty: "intermediate"
estimated_minutes: 25
prerequisites: []
tags: ["episodic-memory", "semantic-memory", "mem0"]
status: "published"
---

# 49.2 — Long-Term Episodic & Semantic Memory with Vector Stores

## Opening Motivation & Context

In the realm of autonomous agents and AI systems, memory is a critical component that enables learning, reasoning, and decision-making. Long-term memory can be broadly categorized into two types: episodic memory, which stores specific events or experiences, and semantic memory, which stores general knowledge and facts. Vector stores, also known as vector databases, are specialized data structures that efficiently store and retrieve high-dimensional vectors, making them ideal for implementing both episodic and semantic memory in AI systems.

## Core Concepts Breakdown

### Vector Stores

Vector stores are databases designed to store and retrieve high-dimensional vectors. These vectors can represent various types of data, such as text, images, or audio, and are used to capture the semantic meaning or features of the data. Vector stores use similarity search algorithms to find vectors that are close to a given query vector, enabling efficient retrieval of relevant information.

> [!KEY-INSIGHT]
> Vector stores provide an efficient way to store and retrieve high-dimensional vectors, enabling the implementation of both episodic and semantic memory in AI systems.

> [!BEST-PRACTICE]
> When designing a vector store, consider the dimensionality of the vectors, the number of vectors to be stored, and the type of similarity search algorithm to be used. Additionally, ensure that the vector store is scalable and can handle large volumes of data.

> [!WARNING]
> High-dimensional vectors can lead to the "curse of dimensionality," where the distance between vectors becomes less meaningful. To mitigate this, consider using dimensionality reduction techniques or specialized similarity search algorithms designed for high-dimensional data.

## Code Demonstration & Runtime Output

```python
import numpy as np
from typing import List, Tuple

class VectorStore:
    def __init__(self):
        self.vectors: List[np.ndarray] = []
        self.metadata: List[dict] = []

    def add_vector(self, vector: np.ndarray, metadata: dict = None) -> None:
        """Add a vector and its associated metadata to the store."""
        self.vectors.append(vector)
        self.metadata.append(metadata if metadata is not None else {})

    def search(self, query_vector: np.ndarray, k: int = 1) -> List[Tuple[np.ndarray, dict]]:
        """Search for the k most similar vectors to the query vector."""
        if not self.vectors:
            return []

        # Calculate the cosine similarity between the query vector and all vectors in the store
        similarities = [self._cosine_similarity(query_vector, vec) for vec in self.vectors]

        # Get the indices of the k most similar vectors
        top_k_indices = np.argsort(similarities)[-k:][::-1]

        # Return the top k vectors and their associated metadata
        return [(self.vectors[i], self.metadata[i]) for i in top_k_indices]

    @staticmethod
    def _cosine_similarity(a: np.ndarray, b: np.ndarray) -> float:
        """Calculate the cosine similarity between two vectors."""
        dot_product = np.dot(a, b)
        norm_a = np.linalg.norm(a)
        norm_b = np.linalg.norm(b)
        return dot_product / (norm_a * norm_b)

if __name__ == "__main__":
    # Create a vector store
    store = VectorStore()

    # Add some vectors to the store
    store.add_vector(np.array([1, 2, 3]), {"text": "This is a sample vector."})
    store.add_vector(np.array([4, 5, 6]), {"text": "This is another sample vector."})
    store.add_vector(np.array([7, 8, 9]), {"text": "This is yet another sample vector."})

    # Search for the most similar vector to a query vector
    query_vector = np.array([1, 2, 3])
    results = store.search(query_vector, k=2)

    # Print the results
    for vec, meta in results:
        print(f"Vector: {vec}, Metadata: {meta}")
```

```text
Output:
Vector: [1 2 3], Metadata: {'text': 'This is a sample vector.'}
Vector: [4 5 6], Metadata: {'text': 'This is another sample vector.'}
```

### How this works (Line-by-Line Breakdown)

1. **Imports**: The code imports the necessary libraries, including `numpy` for numerical operations and `List` and `Tuple` from the `typing` module for type hints.
2. **VectorStore Class**: The `VectorStore` class is defined to represent a vector store.
3. **`__init__` Method**: The `__init__` method initializes the vector store with empty lists for storing vectors and their associated metadata.
4. **`add_vector` Method**: The `add_vector` method adds a vector and its associated metadata to the store.
5. **`search` Method**: The `search` method searches for the k most similar vectors to a given query vector. It calculates the cosine similarity between the query vector and all vectors in the store, gets the indices of the k most similar vectors, and returns the top k vectors and their associated metadata.
6. **`_cosine_similarity` Method**: The `_cosine_similarity` method calculates the cosine similarity between two vectors.
7. **`__main__` Block**: The `__main__` block demonstrates the usage of the `VectorStore` class. It creates a vector store, adds some vectors to the store, searches for the most similar vectors to a query vector, and prints the results.

> [!ADVANCED]
> The `VectorStore` class uses a list to store vectors and their associated metadata. This approach is simple and easy to understand but may not be the most efficient for large-scale applications. In such cases, consider using specialized vector stores or databases designed for high-dimensional data.

## Summary & Key Takeaways

- Vector stores are specialized data structures that efficiently store and retrieve high-dimensional vectors.
- Vector stores use similarity search algorithms to find vectors that are close to a given query vector.
- When designing a vector store, consider the dimensionality of the vectors, the number of vectors to be stored, and the type of similarity search algorithm to be used.
- High-dimensional vectors can lead to the "curse of dimensionality," where the distance between vectors becomes less meaningful.
- The `VectorStore` class demonstrates a simple implementation of a vector store using a list to store vectors and their associated metadata.

## Quiz time

### Question #1: What is the purpose of a vector store?

<details><summary>Show Solution</summary>

A vector store is a specialized data structure that efficiently stores and retrieves high-dimensional vectors. These vectors can represent various types of data, such as text, images, or audio, and are used to capture the semantic meaning or features of the data. Vector stores use similarity search algorithms to find vectors that are close to a given query vector, enabling efficient retrieval of relevant information.

</details>

### Question #2: What does the following code output?

```python
import numpy as np

def cosine_similarity(a: np.ndarray, b: np.ndarray) -> float:
    dot_product = np.dot(a, b)
    norm_a = np.linalg.norm(a)
    norm_b = np.linalg.norm(b)
    return dot_product / (norm_a * norm_b)

if __name__ == "__main__":
    a = np.array([1, 2, 3])
    b = np.array([4, 5, 6])
    similarity = cosine_similarity(a, b)
    print(f"Cosine similarity: {similarity:.2f}")
```

<details><summary>Show Solution</summary>

The code calculates the cosine similarity between two vectors, `a` and `b`. The cosine similarity is a measure of the angle between the two vectors, with a value of 1 indicating that the vectors are identical, and a value of 0 indicating that the vectors are orthogonal. The output of the code is:

```text
Cosine similarity: 0.97
```

</details>

### Question #3: Find and fix the bug in the following code:

```python
import numpy as np
from typing import List, Tuple

class VectorStore:
    def __init__(self):
        self.vectors: List[np.ndarray] = []
        self.metadata: List[dict] = []

    def add_vector(self, vector: np.ndarray, metadata: dict = None) -> None:
        self.vectors.append(vector)
        self.metadata.append(metadata)

    def search(self, query_vector: np.ndarray, k: int = 1) -> List[Tuple[np.ndarray, dict]]:
        if not self.vectors:
            return []

        similarities = [self._cosine_similarity(query_vector, vec) for vec in self.vectors]
        top_k_indices = np.argsort(similarities)[-k:][::-1]
        return [(self.vectors[i], self.metadata[i]) for i in top_k_indices]

    @staticmethod
    def _cosine_similarity(a: np.ndarray, b: np.ndarray) -> float:
        dot_product = np.dot(a, b)
        norm_a = np.linalg.norm(a)
        norm_b = np.linalg.norm(b)
        return dot_product / (norm_a * norm_b)

if __name__ == "__main__":
    store = VectorStore()
    store.add_vector(np.array([1, 2, 3]), {"text": "This is a sample vector."})
    store.add_vector(np.array([4, 5, 6]), {"text": "This is another sample vector."})
    store.add_vector(np.array([7, 8, 9]), {"text": "This is yet another sample vector."})

    query_vector = np.array([1, 2, 3])
    results = store.search(query_vector, k=2)

    for vec, meta in results:
        print(f"Vector: {vec}, Metadata: {meta}")
```

<details><summary>Show Solution</summary>

The bug in the code is in the `add_vector` method, where the `metadata` parameter is not given a default value of `None`. This can lead to unexpected behavior when adding vectors without metadata. The fixed code is:

```python
import numpy as np
from typing import List, Tuple

class VectorStore:
    def __init__(self):
        self.vectors: List[np.ndarray] = []
        self.metadata: List[dict] = []

    def add_vector(self, vector: np.ndarray, metadata: dict = None) -> None:
        self.vectors.append(vector)
        self.metadata.append(metadata if metadata is not None else {})

    def search(self, query_vector: np.ndarray, k: int = 1) -> List[Tuple[np.ndarray, dict]]:
        if not self.vectors:
            return []

        similarities = [self._cosine_similarity(query_vector, vec) for vec in self.vectors]
        top_k_indices = np.argsort(similarities)[-k:][::-1]
        return [(self.vectors[i], self.metadata[i]) for i in top_k_indices]

    @staticmethod
    def _cosine_similarity(a: np.ndarray, b: np.ndarray) -> float:
        dot_product = np.dot(a, b)
        norm_a = np.linalg.norm(a)
        norm_b = np.linalg.norm(b)
        return dot_product / (norm_a * norm_b)

if __name__ == "__main__":
    store = VectorStore()
    store.add_vector(np.array([1, 2, 3]), {"text": "This is a sample vector."})
    store.add_vector(np.array([4, 5, 6]), {"text": "This is another sample vector."})
    store.add_vector(np.array([7, 8, 9]), {"text": "This is yet another sample vector."})

    query_vector = np.array([1, 2, 3])
    results = store.search(query_vector, k=2)

    for vec, meta in results:
        print(f"Vector: {vec}, Metadata: {meta}")
```

</details>
