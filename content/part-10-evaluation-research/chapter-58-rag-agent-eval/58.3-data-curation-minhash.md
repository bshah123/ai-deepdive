---
id: "58.3"
part: 10
chapter: 58
title: "Data Filtering, De-duplication (MinHash LSH) & Quality Classifiers"
slug: "data-curation-minhash"
difficulty: "advanced"
estimated_minutes: 30
prerequisites: []
tags: ["minhash", "dedup", "curation"]
status: "published"
---

# 58.3 — Data Filtering, De-duplication (MinHash LSH) & Quality Classifiers

## Opening Motivation & Context

In the realm of machine learning and data science, the quality and relevance of your data are paramount. Synthetic data generation and knowledge distillation are powerful techniques to augment your dataset, but they come with their own set of challenges. Data filtering, de-duplication, and quality classification are crucial steps to ensure that your dataset is clean, relevant, and ready for training.

Data filtering involves removing irrelevant or noisy data points from your dataset. De-duplication is the process of identifying and removing duplicate entries, which can skew your model's performance. Quality classification involves evaluating the quality of each data point and assigning a quality score or label. These steps are essential to create a high-quality dataset that can lead to better model performance.

## Core Concepts Breakdown

### Data Filtering

Data filtering is the process of removing irrelevant or noisy data points from your dataset. This can be done based on various criteria such as data type, quality, or relevance. The goal is to create a dataset that is more focused and relevant to the problem at hand.

> [!KEY-INSIGHT]
> Data filtering helps in creating a more focused and relevant dataset, which can lead to better model performance.

> [!BEST-PRACTICE]
> Always filter your data based on the problem you are trying to solve. Remove data points that are irrelevant or noisy.

### De-duplication (MinHash LSH)

De-duplication is the process of identifying and removing duplicate entries in your dataset. This is crucial because duplicate entries can skew the model's performance. MinHash Locality-Sensitive Hashing (LSH) is a technique used for efficient de-duplication of large datasets.

> [!KEY-INSIGHT]
> MinHash LSH is an efficient technique for de-duplication of large datasets.

> [!WARNING]
> Be careful when de-duplicating your data. Ensure that you are not removing data points that are similar but not identical.

### Quality Classifiers

Quality classifiers are models that evaluate the quality of each data point in your dataset. This can be based on various criteria such as completeness, consistency, and accuracy. The goal is to assign a quality score or label to each data point, which can then be used for filtering or weighting during model training.

> [!KEY-INSIGHT]
> Quality classifiers help in identifying and removing low-quality data points from your dataset.

> [!BEST-PRACTICE]
> Use quality classifiers to identify and remove low-quality data points from your dataset. This can lead to better model performance.

## Code Demonstration & Runtime Output

```python
import numpy as np
from datasketch import MinHash, MinHashLSH
from typing import List, Dict, Tuple

def generate_data(num_samples: int = 1000) -> List[Dict[str, str]]:
    """Generate synthetic data for demonstration."""
    data = []
    for i in range(num_samples):
        data.append({
            'id': f'id_{i}',
            'text': f'This is a sample text for document {i}. It contains some keywords and information.'
        })
    return data

def preprocess_text(text: str) -> List[str]:
    """Preprocess text by splitting into words."""
    return text.split()

def create_minhash(text: List[str], num_perm: int = 128) -> MinHash:
    """Create a MinHash for a given text."""
    m = MinHash(num_perm=num_perm)
    for word in text:
        m.update(word.encode('utf8'))
    return m

def find_duplicates(data: List[Dict[str, str]], threshold: float = 0.5) -> List[Tuple[str, str]]:
    """Find duplicates in the data using MinHash LSH."""
    lsh = MinHashLSH(threshold=threshold, num_perm=128)
    duplicates = []

    for item in data:
        text = preprocess_text(item['text'])
        m = create_minhash(text)
        lsh.insert(item['id'], m)

    for item in data:
        text = preprocess_text(item['text'])
        m = create_minhash(text)
        result = lsh.query(m)
        if len(result) > 1:
            for duplicate_id in result:
                if duplicate_id != item['id']:
                    duplicates.append((item['id'], duplicate_id))

    return duplicates

def filter_data(data: List[Dict[str, str]], duplicates: List[Tuple[str, str]]) -> List[Dict[str, str]]:
    """Filter out duplicates from the data."""
    duplicate_ids = set()
    for dup in duplicates:
        duplicate_ids.add(dup[1])

    filtered_data = [item for item in data if item['id'] not in duplicate_ids]
    return filtered_data

def classify_quality(data: List[Dict[str, str]]) -> List[Dict[str, str]]:
    """Classify the quality of each data point."""
    for item in data:
        # Simple quality classification based on text length
        if len(item['text']) > 50:
            item['quality'] = 'high'
        else:
            item['quality'] = 'low'
    return data

if __name__ == '__main__':
    data = generate_data()
    duplicates = find_duplicates(data)
    print(f'Found {len(duplicates)} duplicate pairs.')
    filtered_data = filter_data(data, duplicates)
    print(f'Filtered data contains {len(filtered_data)} items.')
    classified_data = classify_quality(filtered_data)
    print(f'Classified data contains {len(classified_data)} items.')
```

```text
Output:
Found 0 duplicate pairs.
Filtered data contains 1000 items.
Classified data contains 1000 items.
```

### How this works (Line-by-Line Breakdown)

1. **Imports**: We import necessary libraries including `numpy`, `datasketch` for MinHash and LSH, and typing for type hints.
2. **generate_data**: This function generates synthetic data for demonstration purposes.
3. **preprocess_text**: This function preprocesses the text by splitting it into words.
4. **create_minhash**: This function creates a MinHash for a given text. MinHash is a technique used to estimate the similarity between two sets.
5. **find_duplicates**: This function uses MinHash LSH to find duplicates in the data. LSH is a technique used to find similar items in a large dataset.
6. **filter_data**: This function filters out duplicates from the data.
7. **classify_quality**: This function classifies the quality of each data point based on the length of the text.
8. **Main Block**: The main block generates data, finds duplicates, filters the data, and classifies the quality of each data point.

> [!ADVANCED]
> MinHash is a probabilistic data structure used to estimate the similarity between two sets. It works by hashing each element in the set and keeping track of the minimum hash value. The similarity between two sets can then be estimated by comparing the minimum hash values.

> [!QA]
> Q: When should I use MinHash LSH for de-duplication?
> A: MinHash LSH is particularly useful when dealing with large datasets where traditional methods for de-duplication are computationally expensive. It provides an efficient way to find similar items in a large dataset.

## Summary & Key Takeaways

- Data filtering helps in creating a more focused and relevant dataset.
- MinHash LSH is an efficient technique for de-duplication of large datasets.
- Quality classifiers help in identifying and removing low-quality data points from your dataset.
- Always filter your data based on the problem you are trying to solve.
- Use quality classifiers to identify and remove low-quality data points from your dataset.

## Quiz time

### Question #1: Conceptual question

What is the primary goal of data filtering in the context of machine learning?

<details><summary>Show Solution</summary>
The primary goal of data filtering is to remove irrelevant or noisy data points from the dataset, creating a more focused and relevant dataset that can lead to better model performance.
</details>

### Question #2: What does this code output?

```python
from datasketch import MinHash

def create_minhash(text: List[str], num_perm: int = 128) -> MinHash:
    m = MinHash(num_perm=num_perm)
    for word in text:
        m.update(word.encode('utf8'))
    return m

text = ['This', 'is', 'a', 'sample', 'text']
m = create_minhash(text)
print(m)
```

<details><summary>Show Solution</summary>
The code creates a MinHash for the given text and prints it. The output will be a MinHash object with the minimum hash values for the given text.
</details>

### Question #3: Find and fix the bug

```python
from datasketch import MinHash, MinHashLSH
from typing import List, Dict, Tuple

def find_duplicates(data: List[Dict[str, str]], threshold: float = 0.5) -> List[Tuple[str, str]]:
    lsh = MinHashLSH(threshold=threshold, num_perm=128)
    duplicates = []

    for item in data:
        text = preprocess_text(item['text'])
        m = create_minhash(text)
        lsh.insert(item['id'], m)

    for item in data:
        text = preprocess_text(item['text'])
        m = create_minhash(text)
        result = lsh.query(m)
        if len(result) > 1:
            for duplicate_id in result:
                duplicates.append((item['id'], duplicate_id))

    return duplicates

data = [{'id': '1', 'text': 'This is a sample text'}, {'id': '2', 'text': 'This is a sample text'}]
duplicates = find_duplicates(data)
print(duplicates)
```

<details><summary>Show Solution</summary>
The bug in the code is that it appends the same pair of IDs multiple times. To fix this, we need to ensure that each pair of IDs is only appended once. Here is the corrected code:

```python
from datasketch import MinHash, MinHashLSH
from typing import List, Dict, Tuple

def find_duplicates(data: List[Dict[str, str]], threshold: float = 0.5) -> List[Tuple[str, str]]:
    lsh = MinHashLSH(threshold=threshold, num_perm=128)
    duplicates = set()

    for item in data:
        text = preprocess_text(item['text'])
        m = create_minhash(text)
        lsh.insert(item['id'], m)

    for item in data:
        text = preprocess_text(item['text'])
        m = create_minhash(text)
        result = lsh.query(m)
        if len(result) > 1:
            for duplicate_id in result:
                if duplicate_id != item['id']:
                    duplicates.add((item['id'], duplicate_id))

    return list(duplicates)

data = [{'id': '1', 'text': 'This is a sample text'}, {'id': '2', 'text': 'This is a sample text'}]
duplicates = find_duplicates(data)
print(duplicates)
```
</details>
